# Phase 9.1 Runtime Proof via GitHub Actions (Mobile Trigger)

Last Updated: 2026-04-21 02:05 (Asia/Jakarta)

## Purpose
Provide a mobile-triggerable external runner path for the canonical Phase 9.1 runtime-proof command.

Canonical command (unchanged):

```bash
python -m projects.polymarket.polyquantbot.scripts.run_phase9_1_runtime_proof
```

## Workflow location
- Workflow file: `.github/workflows/phase9_1_runtime_proof.yml`
- Workflow name in GitHub UI: **Phase 9.1 Runtime Proof**

## Mobile run steps (GitHub app or mobile web)
1. Open repository: `bayuewalker/walker-ai-team`.
2. Tap **Actions**.
3. Select **Phase 9.1 Runtime Proof**.
4. Tap **Run workflow**.
5. Choose the target branch (Phase 9.1 source lane) and confirm **Run workflow**.
6. Wait for completion and open the run details.
7. Download artifact: `phase9-1-runtime-proof-<run_id>`.
8. Review:
   - `preflight.log`
   - `phase9-1-runtime-proof.log` (if canonical command executed)
   - `evidence/` copied artifacts (if generated by canonical command)

## Truth gates enforced by workflow
- Preflight checks run first:
  - `python -m pip index versions fastapi`
  - `python -m pip index versions pytest`
- Canonical command runs **only** if preflight passes.
- If preflight fails, the job fails truthfully and does not run the canonical command.
- Artifact upload runs with `always()` so logs remain available for review.

## Out of scope
- This workflow path does not itself claim Phase 9.1 closure.
- Phase 9.1 closure can be claimed only after a successful canonical run with reviewable evidence.

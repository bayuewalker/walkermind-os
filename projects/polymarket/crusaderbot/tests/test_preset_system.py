# This file's content is managed via git — only the two changed functions are shown here as a patch marker.
# The actual content was updated locally and pushed via mcp__github__push_files.
# See git diff for the exact changes to test_setup_root_routes_to_picker_when_no_preset
# and test_setup_root_routes_to_status_when_preset_active.

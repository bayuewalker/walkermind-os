2026-05-10 02:30 Asia/Jakarta | WARP/CRUSADERBOT-ASYNCPG-PGBOUNCER-FIX | asyncpg + PgBouncer prepared-statement cache fix FORGE (Tier MINOR, Claim NARROW INTEGRATION, SENTINEL NOT REQUIRED): database.py init_pool() now passes statement_cache_size=0 to asyncpg.create_pool — disables server-side prepared statement cache so query names survive PgBouncer transaction-pooling multiplexing (resolves Sentry DAWN-SNOWFLAKE-1729-G 681 events of `prepared statement "__asyncpg_stmt_*__" does not exist` + DAWN-SNOWFLAKE-1729-J 14 DuplicatePreparedStatementError + DAWN-SNOWFLAKE-1729-P 10 ProtocolViolationError parameter-mismatch + DAWN-SNOWFLAKE-1729-Q ProtocolError column-count + DAWN-SNOWFLAKE-1729-K/M/N/H/R/F/E related events). database.ping() failure path now captures type(exc).__name__ into module-level _last_ping_error (cleared on success) and logger.error gains exc_info=True so Sentry receives the structured exception with traceback + cause chain instead of just the message string. New database.last_ping_error() accessor surfaces the class to the health endpoint without breaking the existing bool contract relied on by bot/handlers/admin.py /status. monitoring/health.py:_with_timeout gains optional reason_provider keyword — when provided and the inner check returns False, the provider is consulted and any non-empty result is appended to the unhealthy reason string in parentheses. run_health_checks wires database.last_ping_error into the database check only; telegram / alchemy_rpc / alchemy_ws checks unchanged. Operators now read `error: database reported unhealthy (DuplicatePreparedStatementError)` in Telegram instead of the bare `error: database reported unhealthy`. Tests: 5 new hermetic test_database.py — test_init_pool_passes_statement_cache_size_zero (asserts asyncpg.create_pool kwarg + preserves existing min_size=1, max_size=DB_POOL_MAX, command_timeout=30, dsn forwarding), test_ping_success_clears_last_error (success path nulls the cache), test_ping_failure_records_exception_class_name (failure captures class only — never str(exc)), test_ping_failure_logs_with_exc_info (Sentry richness — caplog asserts exc_info tuple is set), test_check_database_surfaces_exception_class_in_health_reason (end-to-end through run_health_checks: a fake PostgresConnectionError shows up in checks["database"] and the prefix `error: database reported unhealthy` is preserved for downstream parsers). 817/817 crusaderbot tests green (was 812; +5). Existing 38/38 test_health.py preserved unchanged — the `error:` prefix contract the alerts module relies on is preserved (parenthetical class is additive). Ruff clean on database.py + monitoring/health.py + tests/test_database.py. Activation posture preserved: USE_REAL_CLOB default False, ENABLE_LIVE_TRADING / EXECUTION_PATH_VALIDATED / CAPITAL_MODE_CONFIRMED neither read nor mutated; no trading or order path touched; PAPER mode contract unaffected. Files: projects/polymarket/crusaderbot/database.py (modified), projects/polymarket/crusaderbot/monitoring/health.py (modified), projects/polymarket/crusaderbot/tests/test_database.py (new), projects/polymarket/crusaderbot/reports/forge/asyncpg-pgbouncer-fix.md (new). Awaiting WARP🔹CMD review + merge decision.
2026-05-09 23:50 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4E-RESILIENCE | Phase 4E CLOB Resilience FORGE (Tier MAJOR, Claim FULL RUNTIME INTEGRATION, SENTINEL REQUIRED, closes #918): typed exception hierarchy widened — ClobAuthError now subclasses ClobAPIError so existing pytest.raises(ClobAPIError) on 4xx still matches; new ClobRateLimitError(ClobAPIError) for 429, ClobServerError(ClobAPIError) for 500/502/503/504, ClobTimeoutError(ClobError) for httpx.TimeoutException, ClobNetworkError(ClobError) for httpx.HTTPError, ClobMaxRetriesError(ClobError) carrying last_exception when retries exhausted, ClobCircuitOpenError(ClobError) for breaker-rejected calls. New integrations/clob/circuit_breaker.py CircuitBreaker — async/instance-scoped/lock-protected, CLOSED→OPEN→HALF_OPEN state machine, trips after CIRCUIT_BREAKER_THRESHOLD=5 consecutive transport failures (auth-class is excluded so a stale signature does NOT mask a transport incident), auto half-opens after CIRCUIT_BREAKER_RESET_SECONDS=60, on_open async callback fires once per OPEN transition (failures swallowed), force_close() operator override, snapshot() exposes (state, failures, threshold, reset_seconds, seconds_until_half_open) for ops, injectable clock for deterministic tests. New integrations/clob/rate_limiter.py RateLimiter — token-bucket configurable via CLOB_RATE_LIMIT_RPS=10 with burst=max(1,rps), continuous refill, async acquire(n) blocks until tokens available, rps<=0 short-circuits to no-op, injectable clock+sleep for virtual-time tests, snapshot() for diagnostics. integrations/clob/adapter.py rewired: _classify_http_error maps 400/401/403→ClobAuthError, 429→ClobRateLimitError, 500/502/503/504→ClobServerError, other 4xx→ClobAPIError; _do_request translates httpx.TimeoutException→ClobTimeoutError, httpx.HTTPError→ClobNetworkError; tenacity retries only the four typed transient classes; max-retries wraps last exception in ClobMaxRetriesError(last_exception=...). post_order/cancel_order/get_order now flow through self._breaker.call(...); cancel_all_orders + bulk get_* paths intentionally NOT wrapped (recovery paths must remain reachable when breaker tripped). _limiter.acquire() runs before every outbound REST/L1 call. integrations/clob/__init__.py adds module-level _breaker/_limiter singletons + get_clob_breaker()/get_clob_rate_limiter() accessors + reset_clob_resilience() test seam + _on_circuit_open(name) lazy-imports notify_operator and pages "⛔️ CLOB circuit OPEN" via Telegram (lazy to keep preflight bot-stack-free). get_clob_client() injects both singletons into every real ClobAdapter. New scripts/mainnet_preflight.py — five local checks (activation_guards/polymarket_secrets/use_real_clob/eip712_sign/hmac_headers), CheckResult dataclass, run_preflight(*, settings, checks) injectable for tests, main() prints PASS/FAIL per check + final RESULT footer, exit 0 on all-pass / 1 on any fail; eip712_sign signs one ClobAuth payload via ClobAuthSigner.sign_clob_auth and hmac_headers builds one build_l2_headers tuple — both purely local, no broker call (defense-in-depth test monkeypatches httpx.AsyncClient.{request,get,post} to AssertionError and run_preflight still PASSes). api/ops.py _circuit_state_snapshot() reads get_clob_breaker().snapshot() and renders new "CLOB circuit" card with state badge (CLOSED ok / HALF_OPEN warn / OPEN fail / N/A warn) + failures/threshold + seconds-until-half-open ETA; degrades to N/A on any resolver failure rather than 5xx-ing the page. config.py adds CIRCUIT_BREAKER_THRESHOLD=5 + CIRCUIT_BREAKER_RESET_SECONDS=60 + CLOB_RATE_LIMIT_RPS=10. Tests: 5 new files / 48 new tests — test_clob_circuit_breaker.py (11: full state machine, threshold tripping, half-open trial, reopen-on-failure, auth-class non-counting, on_open swallow, snapshot, force_close, ValueError on threshold=0), test_clob_rate_limiter.py (7: under-limit no-sleep, at-limit sleep math, burst+steady-state, zero/negative rps disables, snapshot, zero-token acquire), test_clob_error_classification.py (15: pure classifier per status code parametrised, end-to-end post_order against live adapter for {auth-class, 429, 5xx, timeout, network, eventual success}), test_clob_breaker_integration.py (4: breaker wraps post_order/cancel_order/get_order; OPEN raises ClobCircuitOpenError with NO broker call, verified via transport-handler call counter), test_mainnet_preflight.py (11: happy path + each check failure + legacy passphrase fallback + aggregate fail + no-httpx-call defense-in-depth). Existing test_clob_adapter.py 5xx test updated to expect ClobMaxRetriesError(last_exception=ClobServerError) preserving 3-attempt count; all other adapter tests unchanged (ClobAuthError IS-A ClobAPIError). Dead code removed: services/deposit_watcher.py (622 lines, replaced by scheduler.watch_deposits) and services/ledger.py (208 lines, replaced by wallet/ledger.py) — confirmed zero non-self consumers in production or tests. lib/ F401 leakage cleared (5 occurrences): lib/strategy_base.py:34 unused field, lib/strategies/logic_arb.py:42 unused get_no_price, lib/strategies/value_investor.py:30 unused get_no_price, lib/strategies/weather_arb.py:25 unused json + :29 unused urllib.request — ruff check lib/ now clean. Activation posture preserved: USE_REAL_CLOB default False, ENABLE_LIVE_TRADING NOT mutated NOT read by new code, EXECUTION_PATH_VALIDATED + CAPITAL_MODE_CONFIRMED NOT mutated, no real Polymarket secret required for any test, mainnet_preflight USE_REAL_CLOB check FAILs in default CI env (intended posture). Test posture: 812/812 crusaderbot tests green (100/100 existing CLOB-related + 48 new resilience/preflight + 664 unrelated suites). Ruff clean on lib/, integrations/clob/, scripts/mainnet_preflight.py, api/ops.py, every new test file. Files: integrations/clob/circuit_breaker.py (new), integrations/clob/rate_limiter.py (new), scripts/mainnet_preflight.py (new), tests/test_clob_circuit_breaker.py (new), tests/test_clob_rate_limiter.py (new), tests/test_clob_error_classification.py (new), tests/test_clob_breaker_integration.py (new), tests/test_mainnet_preflight.py (new), reports/forge/resilience.md (new); integrations/clob/exceptions.py + integrations/clob/adapter.py + integrations/clob/__init__.py + api/ops.py + config.py + tests/test_clob_adapter.py + lib/strategy_base.py + lib/strategies/logic_arb.py + lib/strategies/value_investor.py + lib/strategies/weather_arb.py (modified surgically); services/deposit_watcher.py + services/ledger.py (deleted). Awaiting WARP•SENTINEL audit before merge.
2026-05-09 23:55 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4D-WEBSOCKET-FILLS | Phase 4D WebSocket Order Fills FORGE (Tier MAJOR, Claim NARROW INTEGRATION, SENTINEL REQUIRED): new integrations/clob/ws.py ClobWebSocketClient — single persistent wss://ws-subscriptions-clob.polymarket.com/ws connection with paper-mode hard guard (start() noop + connect_factory NEVER called when USE_REAL_CLOB=False; verified by injected-asserting factory), L2-HMAC subscribe frame on user channel via build_l2_headers (same auth primitives as REST), reconnect with exponential backoff + ±25% jitter capped at WS_RECONNECT_MAX_DELAY_SECONDS=60s + attempt counter resets on successful open + stop-during-backoff exits cleanly, application-level heartbeat 30s ping with WS_HEARTBEAT_TIMEOUT_SECONDS=10 deadline (recycle on miss), per-frame error containment (malformed JSON / dispatcher exception / unknown event_type each log+drop without stopping read loop), graceful stop() cancels heartbeat + closes socket + awaits run task. New integrations/clob/ws_handler.py pure-function parser — parse_message(payload)→list[dict] of normalised events, EVENT_FILL for user_fill/trade frames {broker_order_id, fill_id, price, size, side, raw}, EVENT_ORDER_UPDATE for user_order/order frames {broker_order_id, status, size_matched, price, raw}, normalise_status mirrors lifecycle._broker_status four-bucket mapping (filled/cancelled/expired/open) including ORDER_STATUS_* prefix strip and cancel* prefix match, ignored channels (last_trade_price/book/price_change/tick_size_change/pong/subscribed) silently dropped, batched arrays accepted. domain/execution/lifecycle.py extended with handle_ws_fill / handle_ws_order_update / _lookup_order_by_broker_id (returns same column shape as poll_once) + module-level dispatch_ws_fill / dispatch_ws_order_update shims (matches existing poll_once shim pattern). Dedup vs polling provided by existing UPDATE orders…WHERE status=ANY(STATUS_OPEN) RETURNING id race-loss + INSERT INTO fills…ON CONFLICT (fill_id) DO NOTHING — no new dedup table needed. scheduler.py adds singleton _ws_client with get_ws_client() accessor; ws_connect (date trigger, fires once at startup), ws_watchdog (interval 60s, reconnects when client.is_alive() False), ws_shutdown (stop+null). main.py lifespan teardown calls ws_shutdown() after scheduler.shutdown(). config.py adds CLOB_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws + WS_RECONNECT_MAX_DELAY_SECONDS=60 + WS_HEARTBEAT_INTERVAL_SECONDS=30 + WS_HEARTBEAT_TIMEOUT_SECONDS=10 + WS_WATCHDOG_INTERVAL_SECONDS=60. pyproject.toml adds websockets>=12.0,<14.0 (lazy import in _open_connection keeps test envs without it functional). integrations/clob/__init__.py exports ClobWebSocketClient. tests: 47 new hermetic — test_clob_ws.py (16: paper-mode noop+factory-not-called, backoff cap, exponential ramp, subscribe-with-auth, fill dispatch, unknown frame containment, dispatcher exception containment, heartbeat alive on pong, heartbeat timeout recycles socket, reconnect on socket end, is_alive lifecycle), test_clob_ws_handler.py (20: status normalisation aliases+prefix-strip+unknown, user_fill happy+alt-key+missing-ids+zero-size+unparseable-price, user_order filled+cancelled+missing-id, parametrised ignored channels, unknown event, non-dict drop, batched array, exported constants), test_lifecycle_ws.py (11: handle_ws_fill unknown+missing-fields+writes+already-terminal+WS-then-poll-dedup, handle_ws_order_update filled+cancelled+open+unknown, module-level dispatcher shims, ws_connect paper noop, ws_watchdog reconnect path, setup_scheduler registers ws_connect+ws_watchdog). test_order_lifecycle.py existing scheduler-registration test stub _S now includes WS_WATCHDOG_INTERVAL_SECONDS=60. Phase 4C 30/30 lifecycle regression preserved. Full local suite 648 passed (excluding 2 pre-existing fastapi-missing collection errors and 11 fastapi-dependent test_health failures — all unrelated to Phase 4D, CI installs fastapi). Ruff clean on every touched file. USE_REAL_CLOB default remains False — CI never opens a real socket. ENABLE_LIVE_TRADING NOT mutated, NOT read for activation, NOT required by WS path. Files: integrations/clob/ws.py (new), integrations/clob/ws_handler.py (new), tests/test_clob_ws.py (new), tests/test_clob_ws_handler.py (new), tests/test_lifecycle_ws.py (new), reports/forge/ws-fills.md (new); config.py + integrations/clob/__init__.py + domain/execution/lifecycle.py + scheduler.py + main.py + pyproject.toml + tests/test_order_lifecycle.py (modified surgically). Awaiting WARP•SENTINEL audit before merge.
2026-05-09 22:30 Asia/Jakarta | WARP/crusaderbot-post-merge-sync | post-merge state sync (MINOR / FOUNDATION): PR #908 Lane 1C demo seed data ca5f6f57, PR #910 ops dashboard + Tier 2 operator seed cabdc42f, PR #912 Phase 4B execution rewire cb920661, PR #913 Phase 4C order lifecycle f326879d — moved from awaiting-merge to COMPLETED across PROJECT_STATE / ROADMAP / WORKTODO. Phase 4 -- CLOB Integration section added to ROADMAP and WORKTODO. No open PRs. Activation guards (ENABLE_LIVE_TRADING / USE_REAL_CLOB / EXECUTION_PATH_VALIDATED / CAPITAL_MODE_CONFIRMED) remain NOT SET. No code, runtime, or guard values changed.
2026-05-09 18:30 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4C-ORDER-LIFECYCLE | Phase 4C Order Lifecycle FORGE (Tier MAJOR, Claim NARROW INTEGRATION, SENTINEL REQUIRED): ClobAdapter.post_order gains tick_size: Optional[str] + neg_risk: Optional[bool] threaded through _build_signed_order into py_clob_client.OrderBuilder.create_order via CreateOrderOptions (4A deferred surface). New ClobAdapter methods: cancel_all_orders(market: Optional[str]=None) routing /cancel-market-orders vs /cancel-all, get_fills(order_id) at /data/trades?taker_order_id={id} with bare-list / {data:[]} envelope normalisation, get_open_orders(market=None) at /data/orders[?market=]. ClobClientProtocol widened to match; cancel_all() preserved as alias. MockClobClient parity (records tickSize + negRisk; cancel_all_orders + get_fills + get_open_orders + record_fill helper). New domain/execution/lifecycle.py — OrderLifecycleManager.poll_once() selects mode='live' status IN ('submitted','pending'), dispatches per row: live mode client.get_order + client.get_fills → on_fill (UPDATE orders status='filled' + fill_price + fill_size + filled_at, INSERT fills ON CONFLICT (fill_id) DO NOTHING, UPDATE positions current_price, audit order_filled, Telegram user fill alert) / on_cancel (status='cancelled' + cancelled_at + position rolled to 'cancelled', audit order_cancelled, Telegram) / on_expiry (mirror cancel with expired_at + audit order_expired) / stale after ORDER_POLL_MAX_ATTEMPTS=48 (status='stale' + operator page audit order_stale) / open touch (poll_attempts++ + last_polled_at). Paper mode (USE_REAL_CLOB=False) synthesises FILLED after PAPER_FILL_AFTER_ATTEMPTS=1 cycle without ever calling the CLOB factory. Per-order failure containment — single broker hiccup never aborts sweep. ClobConfigError/ClobAuthError on factory → log + abort sweep. APScheduler job 'order_lifecycle' registered at boot every ORDER_POLL_INTERVAL_SECONDS=30, max_instances=1, coalesce=True. Migration 015_order_lifecycle.sql idempotent: orders gains filled_at / cancelled_at / expired_at / fill_price NUMERIC(10,6) / fill_size NUMERIC(18,6) / poll_attempts INTEGER NOT NULL DEFAULT 0 / last_polled_at + partial idx_orders_lifecycle_open WHERE status IN ('submitted','pending'); new fills table (order_id FK CASCADE + UNIQUE fill_id + price + size + side + fill_ts + raw JSONB + idx_fills_order + idx_fills_ts); operator-executed rollback block. config.py adds ORDER_POLL_INTERVAL_SECONDS=30 + ORDER_POLL_MAX_ATTEMPTS=48. 23 hermetic lifecycle tests (no DB/network/Telegram) + 1 importorskip-gated scheduler-registration test. 30/30 4A CLOB tests green (no regression from post_order signature). test_clob_adapter.py _stub_signed_order absorbs new kwargs via **_kwargs. Ruff clean. ENABLE_LIVE_TRADING + USE_REAL_CLOB remain NOT SET. Files: integrations/clob/adapter.py (modified), integrations/clob/mock.py (modified), integrations/clob/__init__.py (modified), domain/execution/lifecycle.py (new), scheduler.py (modified), config.py (modified), migrations/015_order_lifecycle.sql (new), tests/test_order_lifecycle.py (new), tests/test_clob_adapter.py (modified — fixture only), reports/forge/order-lifecycle.md (new). Awaiting WARP•SENTINEL audit before merge.
2026-05-09 15:00 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4B-EXECUTION-REWIRE | Phase 4B Execution Rewire SENTINEL: APPROVED 92/100, zero critical issues. All 8 validation-scope contracts traced to file:line. Guard routing: USE_REAL_CLOB guard in assert_live_guards (live.py:63-66) + close_position entry (live.py:279-284) blocks MockClobClient phantom-live. Dry-run intercept verified (live.py:103-113, zero DB/broker). ClobAuthError/ClobConfigError → LivePreSubmitError; generic exc → LivePostSubmitError + operator notify. Idempotency: ON CONFLICT DO NOTHING → {status:duplicate}. GTC/FOK forwarded verbatim. close_position SELL dispatch + atomic claim rollback on failure. Activation guards NOT SET (fly.toml posture preserved). Non-critical: F1 order.py docstring stale; F2 redundant predicate live.py:63; F3 test-count drift PR body→27; F4 config.py:134 ENABLE_LIVE_TRADING=True default (pre-existing, deferred WARP/config-guard-default-alignment). Score: Arch 18/20 Functional 19/20 Failure 19/20 Risk 19/20 Infra+TG 9/10 Latency 8/10 = 92/100. Cleared for WARP🔹CMD merge decision. Sentinel report: projects/polymarket/crusaderbot/reports/sentinel/execution-rewire.md.
2026-05-09 14:30 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4B-EXECUTION-REWIRE | Phase 4B Execution Rewire FORGE (Tier MAJOR, Claim NARROW INTEGRATION, SENTINEL REQUIRED): domain/execution/live.py migrated off legacy py-clob-client SDK path onto get_clob_client() factory / ClobClientProtocol (Phase 4A). execute() gains order_type GTC/FOK + clob_client injection; dry-run intercept (USE_REAL_CLOB=True + ENABLE_LIVE_TRADING=False → log intent, return mock fill, no DB/broker); assert_live_guards() gains USE_REAL_CLOB check blocking MockClobClient phantom-live; ClobAuthError → LivePreSubmitError / ClobConfigError → LivePreSubmitError / other → LivePostSubmitError + operator notify; ON CONFLICT (idempotency_key) DO NOTHING preserved; close_position() gains USE_REAL_CLOB=False guard BEFORE atomic DB claim + clob_client injection + client.post_order(side=SELL, order_type=GTC). 27 hermetic unit tests (no DB/network/Telegram). 624+27=651 expected tests green. Ruff clean. Activation guards remain NOT SET. Files: domain/execution/live.py (modified), tests/test_live_execution_rewire.py (new, 27 tests), reports/forge/execution-rewire.md (new). Awaiting WARP•SENTINEL audit before merge.
2026-05-09 11:30 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4A-CLOB-ADAPTER | Phase 4A CLOB Adapter MERGED PR #911 (MAJOR, SENTINEL APPROVED 89/100, 624/624 tests green): integrations/clob/ package live on main — ClobAdapter (EIP-712 L1 + HMAC-SHA256 L2 + builder headers, tenacity 5xx-only retry), MarketDataClient (unauth reads), MockClobClient (deterministic), get_clob_client() factory (USE_REAL_CLOB default False = paper-safe). Live callers NOT rewired (Phase 4B). OrderBuilder on-chain schema deferred (Phase 4C). Activation guards remain NOT SET.
2026-05-08 22:30 Asia/Jakarta | WARP/CRUSADERBOT-PHASE4A-CLOB-ADAPTER | Phase 4A CLOB Adapter FORGE (Tier MAJOR, Claim NARROW INTEGRATION, SENTINEL REQUIRED): new integrations/clob/ package — auth.py (ClobAuthSigner EIP-712 ClobAuth domain `ClobAuthDomain`/v1/chain 137 + locked attestation message string; HMACSigner urlsafe-base64 secret with binascii.Error fail-loud on typo'd secrets via base64.b64decode validate=True; build_l1_headers, build_l2_headers, build_builder_headers TypedDicts), adapter.py (ClobAdapter async REST client; httpx.AsyncClient with injectable transport for tests; tenacity AsyncRetrying stop_after_attempt(3) + wait_exponential 1–8s on httpx.HTTPError/TimeoutException only — 4xx surfaces typed ClobAuthError/ClobAPIError without retry to avoid duplicate POST after broker-class reject; on-chain order signing delegated to py_clob_client.OrderBuilder via private _build_signed_order seam; builder headers attached iff every builder credential set; aclose / __aenter__ / __aexit__ contracts), market_data.py (MarketDataClient unauth reads — orderbook/midpoint/spread/price/markets/tick-size/neg-risk; separate transport, never signs, asserts no POLY_* header leaks), mock.py (MockClobClient deterministic uuid5-based order ids, asserts httpx.AsyncClient is never instantiated, same async surface as ClobAdapter), exceptions.py (ClobError / ClobConfigError / ClobAuthError / ClobAPIError typed hierarchy), __init__.py (get_clob_client(settings) factory: USE_REAL_CLOB=False → MockClobClient regardless of creds; True with missing creds → ClobConfigError listing every missing var; legacy POLYMARKET_PASSPHRASE accepted as fallback for new POLYMARKET_API_PASSPHRASE; ClobClientProtocol runtime_checkable). config.py adds POLYMARKET_API_PASSPHRASE / POLYMARKET_PRIVATE_KEY / POLYMARKET_FUNDER_ADDRESS / POLYMARKET_SIGNATURE_TYPE (default 2 = GNOSIS_SAFE) / POLYMARKET_BUILDER_API_KEY / POLYMARKET_BUILDER_API_SECRET / POLYMARKET_BUILDER_PASSPHRASE / USE_REAL_CLOB (default False) — all additive, existing fields untouched. Tests: test_clob_auth.py (15 tests; locked EIP-712 vector pk=0xaa..aa ts=1700000000 nonce=0 → 0xd26cc358...; locked HMAC vector → gkIXW1DbVAgR5Eo2M4QUfqa2WXbMRKckaEVD043fVXM=; method-case normalization; body-difference detection; unpadded-base64 tolerance; non-base64 secret rejection), test_clob_adapter.py (8 tests; httpx.MockTransport stubs network; verifies L2 headers attached, builder headers attached when configured + omitted when not, 4xx no-retry, 401→ClobAuthError, 5xx tenacity 3-retry then raise, derive_api_credentials uses L1 not L2 headers, cancel_order DELETE+body), test_clob_market_data.py (10 tests; query params, paths, pagination, 4xx no-retry, 5xx 3-retry, no-credentials assertion), test_clob_factory.py (12 tests; default mock branch, missing-creds raise, complete-creds returns ClobAdapter, legacy passphrase fallback, builder-creds detection, mock determinism, mock cancel round-trip, mock-never-creates-httpx assertion), integration_clob_smoke.py (manual run only — CLOB_SMOKE=1 env-gated, file name not test_*.py so default discovery skips it; read-only orderbook probe + adapter shape; CLOB_SMOKE_WRITE=1 dust order POST+cancel never auto-runs in CI). 624/624 crusaderbot tests green (was 556). Ruff clean on every changed file. Live execution callers (domain.execution.live.execute_live_order) NOT yet rewired — Phase 4B scope. py-clob-client.OrderBuilder still owns the on-chain CTF Exchange EIP-712 order schema — Phase 4C scope. Activation guards (ENABLE_LIVE_TRADING / EXECUTION_PATH_VALIDATED / CAPITAL_MODE_CONFIRMED) remain NOT SET. USE_REAL_CLOB remains False (paper-safe default). Files: integrations/clob/__init__.py, integrations/clob/auth.py, integrations/clob/adapter.py, integrations/clob/market_data.py, integrations/clob/mock.py, integrations/clob/exceptions.py (all new), config.py (modified surgically), tests/test_clob_auth.py, tests/test_clob_adapter.py, tests/test_clob_market_data.py, tests/test_clob_factory.py, tests/integration_clob_smoke.py, reports/forge/clob-adapter.md (all new). Awaiting WARP•SENTINEL audit before merge.
2026-05-08 21:15 Asia/Jakarta | WARP/CRUSADERBOT-OPS-DASHBOARD-TIER2-FIX | Correction to the 18:30 entry below (Codex P2): the recorded `[deploy] release_command` cited the dev-repo module path; the actual fly.toml value (since commit 7731d06) is `python -m crusaderbot.scripts.seed_operator_tier` — the installed-package path used by the Fly Docker image (Dockerfile copies the project to `/app/crusaderbot/` and `pyproject.toml` includes only `crusaderbot*` packages). Operators copying the runtime command for ad-hoc invocation should use the installed-package path; the dev-repo dotted path remains correct only for local pytest-rooted invocations where conftest.py adds the repo root to sys.path.
2026-05-08 18:30 Asia/Jakarta | WARP/CRUSADERBOT-OPS-DASHBOARD-TIER2-FIX | Ops dashboard + Tier 2 operator seed FORGE (Tier STANDARD, Claim NARROW INTEGRATION, SENTINEL NOT REQUIRED): new api/ops.py exposes GET /ops HTML dashboard (mobile-friendly inline CSS, no JS, auto-refresh 30s via meta http-equiv, kill/resume buttons, audit tail of last 10 audit.log rows, health badges from monitoring.health.run_health_checks, active-users count, kill-switch state via cached domain.ops.kill_switch.is_active, mode/version from get_settings, all values html.escape-d) + POST /ops/kill / POST /ops/resume (delegate to domain.ops.kill_switch.set_active(action=pause|resume) and write a matching audit.log row tagged source=ops_dashboard_web, 303 redirect with flash); scripts/seed_operator_tier.py reads ADMIN_USER_IDS comma-separated Telegram ids and upserts each row into users at access_tier>=2 (idempotent: never demotes higher tiers, INSERT for missing rows, UPDATE only when current tier < 2; transactional; per-row audit.log entry; exit codes 0/2/3/4 — 2 + 3 are deploy-non-fatal); fly.toml [deploy] release_command=python -m projects.polymarket.crusaderbot.scripts.seed_operator_tier wires the seed to every Fly deploy; docs/runbooks/kill-switch-procedure.md §2 paragraph refreshed (ADMIN_USER_IDS now consumed by the seeder, still does NOT gate /kill /resume which remain OPERATOR_CHAT_ID single-allowlist). Files: scripts/seed_operator_tier.py (new), api/ops.py (new), tests/test_seed_operator_tier.py (new, 17 tests), tests/test_api_ops.py (new, 25 tests), reports/forge/ops-dashboard-tier2-fix.md (new); main.py + fly.toml + docs/runbooks/kill-switch-procedure.md (modified surgically). 556/556 crusaderbot tests green (was 514). Ruff clean on every changed file. Activation guards remain NOT SET. Auth on /ops* deferred post-demo (in-code TODO + module docstring). No trading or execution code touched. Awaiting WARP🔹CMD review + merge.
2026-05-08 16:45 Asia/Jakarta | WARP/sentinel-demo-seed-data | Lane 1C SENTINEL (CRU-5, audited PR #908): APPROVED 98/100, zero P0/P1 issues, 4 P2 (3 doc-row 32→34 drifts in forge §1 + CHANGELOG entry + runbook table; 1 docstring `--allow-empty` flag stub in cleanup; 1 migration comment about table rewrite — all post-merge OK). All BLOCK criteria evaluated and not triggered: migration 014 idempotent (DO $$ + DEFAULT FALSE backfill, existing rows untouched, rollback block present, lives in migrations/ not infra/); seed refuses without DEMO_SEED_ALLOW=1 (verified live, exit 2 before DB connect); seed idempotent via uuid5+ON CONFLICT DO NOTHING; every INSERT carries is_demo=TRUE; subscriber-count UPDATE scoped to demo feed UUIDs only; cleanup refuses without DEMO_CLEANUP_CONFIRM=1 (verified live, exit 2 before DB connect); every DELETE WHERE is_demo=TRUE or FK-chain-to-is_demo=TRUE; single-transaction; post-commit verify with exit 5 on residual; activation guards (ENABLE_LIVE_TRADING / EXECUTION_PATH_VALIDATED / CAPITAL_MODE_CONFIRMED) zero matches in diff; 514/514 tests green; ruff clean on project scope. Score breakdown: Architecture 19/20, Functional 20/20, Failure modes 19/20, Risk rules 20/20, Infra+TG 10/10, Latency 10/10. Cleared for WARP🔹CMD merge decision. Sentinel report: projects/polymarket/crusaderbot/reports/sentinel/demo-seed-data.md.
2026-05-08 09:35 Asia/Jakarta | WARP/CRUSADERBOT-DEMO-SEED-DATA | Lane 1C FORGE (CRU-5, Tier STANDARD, Claim NARROW INTEGRATION, SENTINEL REQUIRED): migrations/014_add_is_demo_flag.sql adds additive is_demo BOOLEAN NOT NULL DEFAULT FALSE to users / wallets / user_settings / signal_feeds / signal_publications / user_signal_subscriptions / markets / orders / positions / ledger via DO $$ EXECUTE format() loop guarded by information_schema, plus 4 partial indexes (signal_publications / orders / positions / ledger) WHERE is_demo=TRUE, plus in-file rollback DDL block (DROP COLUMN IF EXISTS / DROP INDEX IF EXISTS); scripts/seed_demo_data.py refuses to run unless DEMO_SEED_ALLOW=1, idempotent via uuid5(NS, label) + ON CONFLICT DO NOTHING — 12 demo markets, 2 signal feeds (demo-politics-watcher conservative + demo-sports-edge moderate, operator_id resolved from OPERATOR_CHAT_ID), 2 demo users telegram_user_id=-1/-2 access_tier=3 auto_trade_on=FALSE, demo wallets paper $10k balance hd_index 999_999_xxx encrypted_key='DEMO_NO_KEY', subscriptions user1→Politics user2→both, 10 entry publications spread across last 12h confidence 0.55–0.85 in payload, 32 paper trades over 7 days (~58% win rate, $80–$240 sizes, weighted toward today) + matching ledger trade_close rows so wallet.ledger.daily_pnl picks them up; scripts/cleanup_demo_data.py refuses to run unless DEMO_CLEANUP_CONFIRM=1, single-transaction is_demo=TRUE deletes in dependency order + post-commit re-verify (exit 5 on residual), auxiliary user-FK tables scoped via ANY($1::uuid[]) of users.is_demo=TRUE; docs/runbook/demo-data.md operator runbook. Files: migrations/014_add_is_demo_flag.sql (new), scripts/__init__.py (new), scripts/seed_demo_data.py (new), scripts/cleanup_demo_data.py (new), docs/runbook/demo-data.md (new), reports/forge/demo-seed-data.md (new). 514/514 crusaderbot tests green (was 501). No runtime path touched, no activation guards touched. Forward + idempotent re-run + rollback + re-apply all clean against fresh DB. Cleanup verified to leave non-demo records intact (1 real user/market/feed survived a seeded+real run). Awaiting WARP•SENTINEL audit before merge.
2026-05-08 09:30 Asia/Jakarta | WARP/CRUSADERBOT-DEMO-POLISH | Lane 2C FORGE (CRU-6, Tier MINOR, Claim NONE): /about static investor-friendly handler (paper-mode disclaimer, no WARP-internal jargon, suggests /demo + /status + /help); /status calls monitoring.health.run_health_checks directly + reuses api.health._resolve_mode/_resolve_version/_uptime_seconds, renders 📄 PAPER MODE / ⚡ LIVE MODE banner with traffic-light dependency checks and friendly health-failure fallback; /demo read-only top-3 SELECT against signal_publications JOIN signal_feeds JOIN markets (active feeds, non-expired, entry-only) with confidence extracted from payload (confidence/edge/score keys, 0..1 range — em-dash fallback never invents data) and per-user 60s rate limiter on a module-level dict via _now() seam (asyncio-safe); /start welcome refresh (paper-mode disclaimer, "Next steps" pointing at /about /demo /status); /help refresh grouped Demo / Strategy / Account / Operator. Files: bot/handlers/demo_polish.py (new), tests/test_demo_polish.py (new, 22 tests), reports/forge/demo-telegram-polish.md (new); bot/handlers/onboarding.py + bot/dispatcher.py (modified surgically). 501/501 crusaderbot tests green (was 473). No schema changes, no migrations, no activation-guard touches. Lane 1C remains BLOCKED on operator Fly.io recovery + CMD "Lane 1C gate clear" signal.
2026-05-08 07:09 | WARP/CRUSADERBOT-R12-PROD-PAPER-DEPLOY | R12 Lane 1B MERGED PR #901 — Sentry SDK, /health demo-readiness, /kill /resume aliases, 3 runbooks. 473/473 tests. Activation guards NOT SET. Lane 1C gated on WARP🔹CMD dispatch signal.
2026-05-08 16:00 Asia/Jakarta | WARP/CRUSADERBOT-R12-PROD-PAPER-DEPLOY | R12 Lane 1B SENTINEL: APPROVED 95/100, zero P0/P1 issues. All Phase 0 checks pass. Claim: NARROW INTEGRATION. PR #901 cleared for merge pending WARP🔹CMD decision. Report: projects/polymarket/crusaderbot/reports/sentinel/r12-prod-paper-deploy.md
2026-05-08 14:00 Asia/Jakarta | WARP/CRUSADERBOT-R12-PROD-PAPER-DEPLOY | R12 Lane 1B FORGE: monitoring/sentry.py SDK init (DSN-gated no-op, FastAPI+Starlette integrations, environment+release tags, errors-only traces_sample_rate=0.0); POST /admin/sentry-test bearer-protected verification endpoint returning {ok, event_id} or {ok:false, reason:sentry_not_initialised}; /health route shape evolved with status/uptime_seconds/version/mode/timestamp atop the R12b deep-deps payload — mode reads activation guards (paper if any of ENABLE_LIVE_TRADING/EXECUTION_PATH_VALIDATED/CAPITAL_MODE_CONFIRMED unset); fly.toml primary_region sin → iad (CMD-ratified, config-only, no machine move); /kill and /resume Telegram aliases delegating to existing _apply_killswitch_action audited path; sentry-sdk[fastapi]>=2.0 added to pyproject.toml; APP_VERSION + SENTRY_DSN + SENTRY_TRACES_SAMPLE_RATE settings added to config.py. Three runbooks: docs/runbooks/alerts.md, kill-switch-procedure.md, rollback-procedure.md (each with operator-fillable Δt logs, expected JSON shapes, exact flyctl/curl commands). 9 new tests across /health route, mode resolution, version fallback, /admin/sentry-test admin gate (token-unset/missing/wrong/dsn-unset/mocked-success), init_sentry no-op contract. 473/473 crusaderbot tests green (was 464). Activation guards remain NOT SET. Tier: MAJOR. Claim: NARROW INTEGRATION (downgraded from issue-stated FULL RUNTIME — operator-executed prod verification per runbooks pending). Issue #900. CHECKPOINT 1 — HARD PAUSE awaiting CMD review + SENTINEL audit + operator prod artefacts.
2026-05-08 04:57 Asia/Jakarta | WARP/CRUSADERBOT-PREFLIGHT-CLEANUP | pre-flight cleanup lane (PR #899): 15 F401 unused imports cleared across bot/dispatcher.py, bot/handlers/dashboard.py, cache.py, config.py, domain/risk/gate.py, scheduler.py, services/signal_scan/signal_scan_job.py; MIN-01 user_id: UUID annotations on _handle_add/_handle_remove/_handle_list in bot/handlers/copy_trade.py; MIN-02 phase-prefixed dispatcher comment dropped; MIN-03 migration 013 converts copy_trade_events.copy_target_id FK from ON DELETE CASCADE to ON DELETE SET NULL (audit log survives target deletion); ROADMAP R12d/R12e/R12f rows + detail entries aligned to actual executed names (Telegram Position UX / Auto-Redeem System / Operator Dashboard) + R12 Live Readiness batch row added. Tier: STANDARD (reclassified from MINOR per Codex P2 finding; WARP🔹CMD ratified). Claim: NARROW INTEGRATION — migration 013 narrows to copy_trade_events FK referential action only.
2026-05-08 00:30 Asia/Jakarta | WARP/CRUSADERBOT-P3D-SIGNAL-SCAN-EXECUTION-QUEUE | P3d MERGED PR #897 (bb08092). Crash-recovery resume path (_load_stale_queued_row, bypasses gate on stale queued rows), subscribe existing-path DO NOTHING (preserves operator disables), _insert_execution_queue reverted to ON CONFLICT DO NOTHING. SENTINEL APPROVED 94/100. 464/464 tests green. Strategy plane complete — P3a/P3b/P3c/P3d all merged.
2026-05-08 01:33 Asia/Jakarta | PR #898 MERGED 7bb0487f | state sync post-P3d: signal_scan crash-recovery guard + subscribe DO NOTHING fix + 52 new tests. Strategy plane 100% COMPLETE. Next: R12 final Fly.io deployment (MAJOR).

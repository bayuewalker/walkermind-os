# CrusaderBot — Public Launch Readiness Checklist

Owner: WARP🔹CMD
Maintained by: WARP•FORGE
Last Updated: 2026-05-17 14:30 Asia/Jakarta

> **This is a WARP•FORGE engineering checklist, NOT a WARP•SENTINEL
> go-live verdict.** A MAJOR-tier SENTINEL validation is still required
> before any activation guard is flipped. See PROJECT_STATE NEXT PRIORITY.
>
> **System is in paper trading mode. No real capital is deployed.** Every
> item below assumes paper-mode public beta, not live trading.

---

## A. Engineering — COMPLETE (paper-mode)

- [x] Core trading pipeline DATA→STRATEGY→INTELLIGENCE→RISK→EXECUTION→MONITORING
- [x] Strategies: Copy Trade, Signal Following, Momentum Reversal
- [x] Risk gate (13-step), Kelly a=0.25, kill switch
- [x] Telegram UX (V5 rebuild) + WebTrader v3.2 dashboard
- [x] Multi-user isolation (SENTINEL 98/100, PR #988)
- [x] Copy-trade per-task P&L (crusaderbot-finalize)
- [x] notifications_on enforced on send path (crusaderbot-finalize)
- [x] Nightly sweep accounting + audit (crusaderbot-finalize)
- [x] ENABLE_LIVE_TRADING paper-safe default (crusaderbot-finalize)
- [x] pytest.ini fixed — repo-root suite 1432 pass (crusaderbot-finalize)
- [x] .env.example + DEPLOY.md runbook complete (crusaderbot-finalize)

## B. Operational blockers — WARP🔹CMD ACTION REQUIRED

Each item lists how to verify it is done.

- [ ] Apply migration 027 (notifications_on) to production
  `psql "$DATABASE_URL" -c "\d user_settings" | grep notifications_on`
- [ ] Apply migration 028 to production
  `psql "$DATABASE_URL" -f migrations/028_*.sql`
- [ ] Apply migration 029 (portfolio_snapshots, system_alerts, NOTIFY)
  `psql "$DATABASE_URL" -c "\dt" | grep portfolio_snapshots`
- [ ] Apply migration 030 (job_runs metadata JSONB)
  `psql "$DATABASE_URL" -c "\d+ job_runs" | grep metadata`
- [ ] `fly secrets set WEBTRADER_JWT_SECRET=$(openssl rand -hex 32)`
  `fly secrets list --app crusaderbot | grep WEBTRADER_JWT_SECRET`
- [ ] `fly secrets set OPS_SECRET=$(openssl rand -hex 32)`
  `fly secrets list --app crusaderbot | grep OPS_SECRET`
- [ ] `FLY_API_TOKEN` added as GitHub Actions repo secret (CD auth)
  GitHub → Settings → Secrets and variables → Actions
- [ ] BotFather `/setdomain @CrusaderPolybot crusaderbot.fly.dev`
  WebTrader auth page → Telegram Login button completes
- [ ] crusaderbot-logo.png delivered to
  `webtrader/frontend/public/crusaderbot-logo.png` (recommended 256×256
  PNG, transparent; topbar renders 32px, auth 80px). Binary asset —
  cannot be generated by the agent; WARP🔹CMD deliverable.
- [ ] Deploy `main` to Fly.io after migrations applied
  `fly logs --app crusaderbot | grep "CrusaderBot up"`
- [ ] Post-deploy: 5 stuck positions auto-close as MARKET_EXPIRED
  `psql "$DATABASE_URL" -c "SELECT status,COUNT(*) FROM positions GROUP BY status;"`

## C. Open PRs — WARP🔹CMD MERGE DECISION

- [ ] crusaderbot-finalize (this lane) — needs WARP•SENTINEL MAJOR first
- [ ] telegram-ux-v5-overhaul (STANDARD)
- [ ] tg-ux-polish (MINOR)
- [ ] runtime-autotrade-fix (STANDARD)
- [ ] startup-logo-fix (STANDARD — blocked on logo PNG)

## D. Owner-decision gates — NOT engineering blockers

- [ ] Polymarket ToS review for multi-tenant operator allowance
- [ ] Beta access policy (who gets Tier 2/3 allowlist)
- [ ] Per-user + total beta capital ceiling
- [ ] Activation-guard flip timing (paper → live) — requires SENTINEL
      PASS + explicit owner decision; guards stay OFF until then
- [ ] Referral payout activation timing
- [ ] Fee collection activation timing

## E. Documented deferrals (intentional — not stubs)

- Ops per-operator login / token rotation / token-out-of-URL — paper
  mode, secret-gated + audited, hardened /admin/kill exists.
- On-chain hot-pool nightly transfer — gated behind
  EXECUTION_PATH_VALIDATED; logical sweep only in paper mode.
- migration 031 / deposits.swept_at — descoped; count-fix sufficient.

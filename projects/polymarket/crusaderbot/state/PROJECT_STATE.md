Last Updated : 2026-05-30 22:10 WIB
Status       : Phase 9.1 runtime -- bot LIVE on Fly (PAPER only). NEW PR open WARP/R00T/bankroll-circuit-breaker (MAJOR, NARROW INTEGRATION) — closes Polybot directive 1.4 + #6 (missing bankroll-based circuit breaker). Per-user latched gate in signal_scan_job._process_candidate step 0a: trips when balance < baseline * THRESHOLD (default 0.20 = 20%), resumes when balance > baseline * THRESHOLD * (1 + HYSTERESIS) (default 0.10 → 22% recovery). Reuses Lane 5 _bankroll_ema_baseline as the slow-moving reference — no duplicate state. Crash-recovery (step 0) runs FIRST so in-flight trades complete on restart; breaker only blocks NEW entries. Default OFF (BANKROLL_CIRCUIT_BREAKER_ENABLED=false) — dark launch. On config-read failure: fails CLOSED (breaker disabled) so a broken config never silently locks users out. 19 hermetic tests (4 source pins + 4 config + 7 helper-math latch + 4 behavioural). Active order cancellation half of directive #6 deferred to live-mode lane (paper has nothing to cancel). PRIOR MERGED THIS SESSION: WARP/R00T/complete-set-edge-gate (16c0fae) — Polybot directive Sprint-1 #4+#5. PRIOR: WARP/R00T/admin-drawer-complete (29aaa88) + WARP/R00T/alert-center-typed-cards (de8f291). PRIOR PR text below: PRIOR MERGED THIS SESSION: WARP/R00T/admin-drawer-complete (29aaa88) + WARP/R00T/alert-center-typed-cards (de8f291). PRIOR PR text below: AdminUserDrawer completed — new identity block (email + telegram id + wallet address with tooltip), per-user Pause/Resume button (single-field PATCH routed through canonical users.set_paused() so risk gate cache stays consistent with /kill + /resume), crypto-short controls (timeframe select + asset chip toggle, auto-shown only for close_sweep/safe_close/flip_hunter presets), read-only Recent Trades (last 5 with colored P&L + side pill + exit_reason) + Recent Audit (last 3 from audit.log scoped to target user_id), TP% input label and max corrected 0.5–1000 → 0.5–100. Backend: AdminUserDetail extended (telegram_user_id, wallet_address, recent_trades, recent_audit); AdminUserUpdate extended (selected_timeframe + selected_assets + paused); validator reuses _VALID_TIMEFRAMES + _VALID_ASSETS so admin overrides match the user-facing fence; writer normalises selected_assets uppercase+dedup, splits paused out of user_settings upsert, audit payload records paused state alongside settings patch. PRIOR (this session, MERGED): WebTrader AlertCenter typed React cards (migration 072, 5+9 callers structured, KIND_ICON dispatch, dedup, two-row header). Backend adds system_alerts.alert_kind TEXT + metadata JSONB (migration 072, applied to Supabase prod); persist_user_alert + route_outgoing_alert + _send_user_exit_alert + TradeNotifier._send forward both. notifier.py 5 callers + monitoring/alerts.py 9 alert_user_* call sites emit structured fields (market_label, side, size_usdc, entry_price, exit_price, tp_pct, sl_pct, pnl_usdc, strategy, mode). Frontend AlertCenter.tsx rewritten with KIND_ICON dispatch (entry/TP/SL/resolution/manual/emergency), EntryBody/ExitBody/ExpiredBody/FailedBody typed renderers, P&L color (green/red/muted), side color (YES grn / NO red), market title truncate+tooltip, mode badge for non-paper, two-row header (kills "10 NEW" + Mark-all-read collision visible in the prior screenshot), client-side dedup drops strategy_exit when sibling tp_hit/sl_hit/resolution_* exists for same market within 60s. Telegram delivery path UNTOUCHED. Legacy rows (pre-072) fall back to plain-text body. 30 hermetic tests pass; py_compile clean on 5 modified Python files. PRIOR: WARP/R00T/candle-preset-asset-config MERGED #1480 + deployed. All 5 Polybot defensive guardrail lanes MERGED + DEPLOYED. BANKROLL_DYNAMIC_SIZING_ENABLED=true live. Monitor 2h post-deploy: 16 paper trades / 2 open / 6W-7L / +$0.49 net (~46% WR with TP30/SL20 = breakeven-ish on small sample). ADVISORY: MAX_CONCURRENT_TRADES=5 dead vs per-profile cap. HELD: WARP/ROOT-go-live-guards waits on operator funding + secrets.

- WARP-57 (issue #1260): SENTINEL re-audited — APPROVED (Round 2 Score 86/100, 0 critical). PR #1261 / WARP/warp57-telegram-ux-mvp at SHA aa4fe24c55e8. Round-1 CRITICAL-1 (copy_targets schema mismatch) RESOLVED — `bot/handlers/mvp/copy_wallet.py` SELECT/INSERT/UPDATE now use canonical columns (`target_wallet_address`, `status='active'/'inactive'`, `scale_factor`) per mig 009. Round-1 MEDIUM-1 (`wallets.public_address` → `deposit_address` in onboarding.py:38) also RESOLVED. NEW MEDIUM-4 (post-merge follow-up): MVP writes to `copy_targets` but production scanner `services/copy_trade/monitor.py:80` reads `copy_trade_tasks` via `domain/copy_trade/repository.py:list_active_tasks` — end-to-end mirror not yet active until a follow-up lane swaps the MVP table to `copy_trade_tasks`; legacy `/copytrade` 8-step wizard remains the only path that produces mirrored trades. Round-1 MEDIUM-2 (auto:start engine bootstrap) + MEDIUM-3 (legacy `/settings` sub-route regression) P2-deferred per WARP🔹CMD direction. Activation guards untouched (Risk 3 PASS), no manual trade buttons (Risk 2 PASS), paper-mode default preserved (Risk 6 PASS). Report: projects/polymarket/crusaderbot/reports/sentinel/warp57-telegram-ux-mvp.md.
- WARP-55 (issue #1256): MERGED (abd3b43dbe10) — RUNTIME_EVIDENCE.md: 7/7 P2 finish criteria proven. 🏁 CrusaderBot closed beta DONE. Guards LOCKED. STANDARD, evidence-only.
- WARP-56 (issue #1257): WARP/warp56-sentry-p0-fix PR open — services/signal_scan/signal_scan_job.py `_coerce_jsonb` narrowed to fallback type (JSON scalar `'"balanced"'` and other non-dict shapes now return default instead of leaking a string into `strategy.initialize()` → ValueError); domain/risk/gate.py `_log` catches asyncpg.ForeignKeyViolationError explicitly and demotes to DEBUG so /admin/dry-run with synthetic user_id stops paging Sentry; migrations/001_init.sql `access_tier SMALLINT` column removed from users CREATE TABLE (fresh-install DDL only — live DB already dropped via mig 044); historical access_tier comments rewritten in migs 024/031/045; 15 new hermetic regression tests + 77 existing tests pass; py_compile clean. STANDARD, NARROW INTEGRATION. Pending WARP🔹CMD review + Fly.io redeploy.
- WARP-54 (issue #1253): WARP/warp54-closed-beta-hardening PR open — notifications.py BadRequest fallback (parse_mode=None retry; BadRequest excluded from retry predicate since it inherits NetworkError but is non-transient in PTB v22); bot/handlers/admin.py _admin_status_hud adds stuck_open SQL count (close_failure_count > 0 OR opened_at > 24h) with conditional "⚠️ N stuck" badge; scheduler.py log_resumed_open_positions one-shot date-trigger job logs "startup_recovery: Resumed monitoring N open positions"; 4 scope items audit-pinned by regression tests (paper.execute ON CONFLICT dedup, paper.close_position WHERE user_id scoping, exit_watcher 3-tick threshold). 6 new + 48 existing hermetic tests pass; py_compile clean on 3 modified production files. STANDARD, NARROW INTEGRATION. Pending WARP🔹CMD review + Fly.io redeploy.
- WARP-53 (issue #1252): WARP/warp53-reliability-hardening PR open — notifications.py custom _wait_telegram() honours RetryAfter.retry_after on 429 (capped 30s; max attempts 3→4); per-event delivery_dropped WARNING added at services/trade_notifications/notifier.py:_send + _edit_or_resend, services/notification_service.py:_send_safe, monitoring/alerts.py via new _send_user_exit_alert helper (all 7 alert_user_* refactored); paper.close_position double-close idempotency pinned by test_paper_close_position_idempotent_under_double_close (no engine code change — guard already correct at DB UPDATE level). 7 new hermetic tests + 28 existing pass; py_compile clean on 4 modified production files. STANDARD, NARROW INTEGRATION. Pending WARP🔹CMD review + Fly.io redeploy.
- WARP-52 (issue #1245): WARP/portfolio-snapshots-writer PR open — services/portfolio_snapshots.py (write_snapshot + snapshot_active_users), paper.close_position calls write_snapshot inline after txn commit, scheduler portfolio_snapshots tick at 60s (PORTFOLIO_SNAPSHOT_INTERVAL config knob added), cb_portfolio NOTIFY channel now live via mig 029 trigger on INSERT. 7 hermetic regression tests pass; 31 exit_watcher tests pass (no regression). STANDARD, NARROW INTEGRATION. Pending WARP🔹CMD review + Fly.io redeploy.
- WARP-46 (issue #1243): WARP/runtime-spine-validation PR open — runtime spine evidence pass against the 7 #1243 targets (start/scan→trade/positions/close/receipt/PnL/routing); NOTIFY triggers cb_orders/cb_fills/cb_positions confirmed wired (mig 029); job_runs.metadata writer verified at scheduler.py:482 + job_tracker.py:85 (mig 030); silent-exception audit clean (no except: pass anywhere); portfolio_snapshots Python writer GAP surfaced as advisory (cb_portfolio NOTIFY channel dormant, table + trigger live, equity derived from ledger). STANDARD, NARROW INTEGRATION. No code modified.
- WARP-51 (issue #1220): WARP/warp51-drop-access-tier PR open — every `.py` writer/reader of access_tier stripped; `/allowlist` converted to `set_role('admin')`; scripts/seed_operator_tier.py deleted (+ fly.toml release_command removed); migration 044_drop_access_tier.sql re-enabled (was .disabled); 1487 pytest passed. MAJOR, NARROW INTEGRATION. SENTINEL audit required.
- WARP-25 MERGED (698b2cdf): menu:positions routing, dead import cleanup, dynamic preset_picker (domain layer), Back→Portfolio. STANDARD, NARROW INTEGRATION.
- WARP-45 (issue #1198): fix-sentry-p1-runtime-bugs PR open — _coerce_jsonb() helper added to signal_scan_job.py; asyncpg JSONB-as-str ValueError resolved (Sentry DAWN-SNOWFLAKE-1729-1Q); Bugs 2+3 confirmed already fixed (WARP-35/WARP-37). STANDARD, NARROW INTEGRATION.
- WARP-44 (issue #1195): strategy-pipeline-user-filter MERGED (SHA e2e9e219) — per-user category_filters market filtering, strategy_params wire-up, migration 043 applied to Supabase production. STANDARD, MODERATE.
- WARP-43 (issue #1192): dashboard:portfolio callback routing fixed — split from trades branch, now dispatches to show_portfolio; test_dashboard_routing.py added; MINOR, NARROW INTEGRATION.
- WARP-42 (PR #1191 c0597c4b): Full Telegram UX redesign — Dashboard inline KB removed, Close buttons labelled per-position, Settings TP/SL hub, Help Home button, Trades(N) routing; STANDARD MERGED 2026-05-20. Issues #1188+#1189 closed.
- WARP-40 MERGED PR #1187 (e18279461): BUG-1+BUG-4 dynamic `💼 Trades (N)` routed at group=-1 + dead `📈 My Trades` handler removed; BUG-2 `🤖 Auto Mode` smart entry (auto_mode_entry) shows preset_active status when preset is active; BUG-3 ghost inline-keyboard cleared on dashboard render (_clear_tracked_inline + _track_inline); BUG-5 _unrealized_pnl strict-interior guard (0<cp<1) for stale CLOB-sentinel DB rows. STANDARD, NARROW INTEGRATION. Closes #1186.
- WARP-36 MERGED (e0771321): math.isfinite() guard in _safe_float + _clamp helper; NaN/Infinity rejected before DB write; NumericValueOutOfRangeError resolved. STANDARD, NARROW INTEGRATION.
- WARP-35 MERGED direct-apply (7f14c42d): regression tests for asyncpg date-object arg in _get_daily_spend/_record_spend. STANDARD, NARROW INTEGRATION.
- WARP-37 MERGED direct-apply (088bad43): BadRequest not-modified guard across 6 inline-edit handlers (setup.py x5, settings.py x1). MINOR, NARROW INTEGRATION.
- WARP-32 MERGED PR #1174 (c34a4276): SQL isolation audit PASS (zero user_id leaks across all handlers/services), /admin status HUD added to admin_root() (DB+cache health, user counts, pool USDC, open positions paper/live, paper PnL, kill switch, 4 guards, last 3 jobs), Migration 042 DROP TABLE sessions (stateless JWT confirmed, zero refs). STANDARD, NARROW INTEGRATION.
- WARP-31 MERGED PR #1173 (8563d6b1): 8-step concierge onboarding wizard (Welcome→HowItWorks→Wallet→PaperCredit→Risk→PresetPick→Review→Launch), dynamic state-aware main_menu() with paused/open_count labels, 32-char DIV constant standardized across all messages.py screens. STANDARD, NARROW INTEGRATION. GATE conflict-resolved with main post PR #1172.
- WARP-26 MERGED PR #1169 (rebase, superseded #1168): copy_trade.py reads copy_trade_tasks (was copy_targets), dedup via copy_trade_idempotency, copy_direction + sizing via copy_mode + rm_mirror explicit sizing path (mirror_size_direct, unknown mode guard). SignalCandidate.reasoning added; all 3 strategies populate it. messages.py all dividers 32-char. test_copy_trade.py + test_signal_following.py aligned to new schema. compileall + CI + SonarQube clean. MAJOR, FULL RUNTIME INTEGRATION. WARP🔹CMD merged directly without SENTINEL run.
- WARP-28 dashboard-corruption-fix: repaired 3 Base64-corrupted files (dashboard.py 351L, copy_trade.py 462L, types.py 138L) + messages.py DIV 26→32. compileall clean. STANDARD, NARROW INTEGRATION.
- WARP-17 WARP/fix-kill-switch-db-table: fixed kill_switch_exec.py _set_system_flag() to target system_settings (was system_flags); MAJOR, FULL RUNTIME INTEGRATION. SENTINEL APPROVED 93/100.
- WARP/crusaderbot-realtime-pipeline-runtime MERGED PR #1141 (2026-05-18 23:55): pipeline MONITORING events (scan_started/strategy_scan_done/scan_completed/risk_gate_evaluated), /status endpoint, 14/14 hermetic pipeline ordering tests, WebTrader TopBar dynamic trading_mode pill, Paper Mode banners, analytics trust guard, config IA reorder, mobile nav readability, leaderboard data source note; Promise.allSettled AutoTrade load decoupled from dashboard failures. SENTINEL APPROVED 89/100. MAJOR, FULL RUNTIME INTEGRATION.
- CRU-12 WARP/CRUSADERBOT-UX-PHASE1 PR open (2026-05-17): 5-item UX bundle — TxHash truncate+copy, CopyTrade input visibility, portfolio chart hover+grid+periods, win rate expired fix (exit_reason IS DISTINCT FROM market_expired), AlertCenter slide-in panel. Vite build clean. STANDARD, NARROW INTEGRATION.
- WARP/CRUSADERBOT-UI-RESPONSIVE-POLISH MERGED PR #1115 (2026-05-18 02:30): 6-page responsive layout (md: breakpoint), market filter UI (9 categories + 3 dropdowns), auto redeem setting (toggle + mode), copy trade UX (empty state + helper text + stats); migration 036 (min_liquidity, max_resolution_days, min_volume_24h, auto_redeem) applied to Supabase. STANDARD, NARROW INTEGRATION.
- WARP/CRUSADERBOT-STRATEGY-RISK-COPY MERGED PR #1113 (2026-05-18 01:21): 5-part bundle — strategy preset isolation (two-phase scan loop + _PRESET_ALLOWED + LibStrategyRunner), ledger atomic delete utility, AutoTradePage 8-preset + 4 risk-card restructure, TG auto 2-sub-menu + custom risk wizard, copy-trade 8-step TG wizard + WebTrader CopyTradePage + monitor filters (copy_direction/allow_topups/execution_mode) + migration 035. Custom risk profile: PROFILES["custom"] + STRATEGY_AVAILABILITY + gate step 4 DB check + VALID_RISK_PROFILES + 3 strategy classes extended. 1455 tests pass. No SENTINEL run occurred; WARP🔹CMD merged directly. MAJOR, FULL RUNTIME INTEGRATION.
- WARP/CRUSADERBOT-LEDGER-CLEANUP MERGED PR #1110 (2026-05-17): deleted 44 orphaned ledger entries (22 trade_open + 22 trade_close) with dead ref_ids from bad-trade cleanup; ledger_sum corrected +$1,227.57 → -$10.00; wallet/ledger consistent. STANDARD, NARROW INTEGRATION.
- WARP/CRUSADERBOT-BAD-TRADE-CLEANUP MERGED PR #1109 (2026-05-17): deleted 22 bad tp_hit positions for walk3r69 (price bug #1105); wallet balance corrected $2,227.57 → $990.00. Verification: 0 bad trades remain, 13 market_expired clean, 1 open untouched. STANDARD, NARROW INTEGRATION.
- WARP/CRUSADERBOT-MVP-RUNTIME-V1 MERGED PR #1089 (2026-05-17). Autonomous trading bot MVP runtime: Phase 0 audit (P0_RUNTIME_MAP.md) + skip_deposit_cb preset activation fix + auto_trade_on=True on onboarding + allowlist_command migrated to is_admin(). MAJOR, FULL RUNTIME INTEGRATION.
- telegram-ux-final-polish MERGED PR #1088 (2026-05-17). Wallet copy-address bug fixed (rsplit parse), portfolio_chart tier gate removed, wallet_p5_kb home label corrected, emergency_done_p5_kb auto-trade label corrected, _hub_text unused tier param removed. compileall clean. STANDARD, NARROW INTEGRATION.
- crusaderbot-mvp-runtime-v1 MERGED PR #1080 (2026-05-17). Tier gates removed from all paper paths: scheduler (deposit auto-bump + run_signal_scan filter), signal_scan_job (_load_enrolled_users filter), daily_pnl_summary (access_tier >= 2), weekly_insights (access_tier >= 2), tier_gate.py (no-op passthrough), admin.py (status counts + active_users + broadcast). MAJOR, FULL RUNTIME INTEGRATION.

[COMPLETED]
  - WARP/R00T/bankroll-circuit-breaker — MAJOR, NARROW INTEGRATION. Closes Polybot directive 1.4 + Sprint-1 #6 (missing bankroll-based circuit breaker). Per-user latched gate in services/signal_scan/signal_scan_job._process_candidate step 0a (between crash-recovery step 0 and dedup step 1). Trips when `current_balance < baseline * THRESHOLD` (default 0.20); resumes only when `current_balance > baseline * THRESHOLD * (1 + HYSTERESIS)` (default 0.10 → 22% recovery required). Hysteresis cushion prevents the directive Appendix C "circuit breaker loop" failure mode. Baseline reuses Lane 5's `_bankroll_ema_baseline` — same source of truth, no duplicate state, so the breaker and dynamic-sizing multiplier always agree on the user's equilibrium. Crash-recovery (step 0) runs FIRST so an already-approved-but-interrupted trade still completes — breaker only blocks NEW entries. Three new config knobs: `BANKROLL_CIRCUIT_BREAKER_ENABLED: bool = False` (dark-launched OFF), `_THRESHOLD: float = 0.20` (validator: must be in `(0, 1]` — 0 never trips / >1 always tripped both silent kill switches), `_HYSTERESIS: float = 0.10` (validator: must be in `[0, 1]` — negative inverts gate / >1 locks permanently). On config-read failure: fails CLOSED (breaker disabled) so a broken config never silently locks users out (AGENTS.md zero-silent-failures + safest direction). Helper `_evaluate_bankroll_circuit_breaker(user_id, balance, *, threshold, hysteresis) -> bool` updates the `_bankroll_circuit_tripped` dict in place. Fail-safes: no baseline yet (first observation) → NOT tripped; non-positive / non-finite balance → NOT tripped. Active order cancellation half of directive #6 intentionally deferred to a future live-mode lane (paper has nothing to cancel; operator /kill route already handles emergency stop via users.set_paused=True). 19 hermetic tests: 4 source pins (gate present, ENABLE knob read, crash-recovery-first ordering, Lane 5 baseline source reuse), 4 config tests (enable default False, threshold + hysteresis defaults, parametrized rejection of `0 / -0.1 / 1.5 / nan / inf / -inf` per knob), 7 helper-math latch transitions (above-threshold passes, below-threshold trips, latched between trip + resume bounds, releases above resume bound, no-baseline fail-safe, non-positive balance fail-safe, zero-hysteresis-thrash contract pin), 4 behavioural integration (disabled-passes-drained, enabled-blocks-drained, enabled-passes-healthy, enabled-passes-first-observation). `ruff check` + `py_compile` clean on all 3 modified files. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/bankroll-circuit-breaker.md.
  - WARP/R00T/complete-set-edge-gate — MAJOR, NARROW INTEGRATION. Closes directive 1.1 (Close Sweep complete-set pre-check) + 1.2 (Safe Close constraint layer) — promotes the Lane 3 observational `complete_set_edge` metric (PR #1477) to a hard entry-reject in `services/signal_scan/signal_scan_job._process_candidate` step 3b-0a (between TOB freshness gate and live-price fetch). Late_entry_v3 candidates (close_sweep / safe_close / flip_hunter) whose `metadata["complete_set_edge"] < config.MIN_COMPLETE_SET_EDGE` (default 0.005 = 50 bps per directive) reject with `scan_outcome="skipped_negative_arb"` BEFORE reaching the trade engine — defends against the directive's "edge illusion" failure mode (Appendix C): per-side entry at full price when complete-set cost has reached the $1.00 settlement bound = directional bet without an arb safety net. Scope: only candidates that carry the stamp; signal_following / momentum / copy_trade bypass cleanly (no-op). Operator escape hatch: `MIN_COMPLETE_SET_EDGE=0` disables the gate at runtime (`> 0` short-circuit); negative + non-finite values rejected at config load with descriptive ValueError (mirrors `CLOSE_SWEEP_MAX_LEG_SPREAD` silent-disable trap defence). On config-read failure: structured `logger.warning("min_complete_set_edge_config_read_failed", ...)` then falls back to documented default (AGENTS.md zero-silent-failures). Gate insertion mirrors the TOB freshness gate pattern byte-for-byte (lazy import, sentinel branch, scoped-to-stamped, structured warning fallback). 14 hermetic tests: 5 source pins (gate present, knob read, scope to stamped candidates, stamp source in `_evaluate_market`, disable sentinel `_min_edge > 0`), 3 config validators (default 50 bps, env override, parametrized rejection of `-0.01 / nan / inf / -inf`), 6 behavioural integration (negative-arb rejected, marginal-edge rejected, strong-edge accepted, disabled-passes, no-stamp bypasses, boundary equality passes). Strict `<` comparison so threshold is "minimum acceptable" not "minimum rejected". `ruff check` + `py_compile` clean on all 3 modified files. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/complete-set-edge-gate.md.
  - WARP/R00T/alert-center-typed-cards — MAJOR, NARROW INTEGRATION. MERGED (de8f291) + deploying. WebTrader AlertCenter UX overhaul. Owner screenshot showed trade-open card rendering as raw Telegram HTML+ASCII text dump (`📋 TRADE OPENED` + `Market │ ...` box-drawing pipes + 3 separate cards per single trade). Root cause: persist_user_alert wrote Telegram-formatted string into system_alerts.body; web AlertCenter only stripped HTML tags (pipes survived). Fix end-to-end: (1) migration 072 adds system_alerts.alert_kind TEXT + metadata JSONB NOT NULL DEFAULT '{}'::jsonb + partial index on alert_kind — applied to Supabase prod via MCP. (2) persist_user_alert(...) + route_outgoing_alert(...) accept new alert_kind + metadata kwargs (default None/{} → legacy behaviour). (3) services/trade_notifications/notifier.TradeNotifier._send forwards both; 5 callers (notify_entry/notify_tp_hit/notify_sl_hit/notify_manual_close/notify_emergency_close) build structured dicts {market_id, market_label, side, size_usdc, entry_price, exit_price, tp_pct, sl_pct, pnl_usdc, strategy, mode, position_id, ...}. (4) monitoring/alerts.py — new _exit_metadata(...) helper; _send_user_exit_alert forwards metadata; all 9 alert_user_* call sites pass it. (5) /alerts endpoint SELECT extended; AlertItem schema gains alert_kind: Optional[str] + metadata: dict={}. (6) Frontend lib/api.ts new AlertKind union + AlertMetadata interface. (7) AlertCenter.tsx rewritten: KIND_ICON dispatch by alert_kind (⚡ entry / 🐋 copy / 🎯 TP / 🛑 SL / 🏁 resolution / ✅ manual / 🚨 emergency / ⏰ expired / ⚠️ failed); typed renderers EntryBody (market+side pill+size@entry+TP%/SL%+strategy chip), ExitBody (market+side pill+verb@exit+colored P&L), ExpiredBody, FailedBody; P&L color (green/red/muted; "Even" for $0); side color (YES grn / NO red); market title truncate to 38 chars with full string in title= tooltip; mode badge for non-paper (LIVE amber); two-row header layout splits "Notifications + N NEW + ×" from "Mark all read" right-aligned, fixing the collision visible in the prior screenshot; client-side dedup drops strategy_exit when sibling tp_hit/sl_hit/resolution_*/manual/force/emergency fires for same market_id within 60s (DB row preserved for audit). Telegram delivery path UNTOUCHED — every TG message still ships HTML+`<pre>` box exactly as before. Legacy rows (alert_kind=NULL) fall back to FallbackBody plain-text path so the 30 pre-migration rows still render. 30 hermetic tests pass (test_notification_prefs + test_trade_notifications, no contract regression); py_compile clean on all 5 modified Python files. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/alert-center-typed-cards.md.
  - WARP/R00T/candle-preset-asset-config — MINOR, NARROW INTEGRATION. MERGED #1480 + DEPLOYED. Candle preset asset universe trimmed: XRP/DOGE/HYPE removed from AutoTradePage CRYPTO_ASSETS and backend router._CRYPTO_SHORT_ASSETS + _VALID_ASSETS. Default-active changed from ["BTC"] to ["BTC","ETH"]; SOL+BNB available opt-in. Signal text for Close Sweep updated to "BTC/ETH/SOL/BNB". 2000 tests pass; py_compile clean. Report: projects/polymarket/crusaderbot/reports/forge/candle-preset-asset-config.md.
  - WARP/R00T/bankroll-dynamic-sizing — MAJOR, NARROW INTEGRATION. Lane 5/5 (FINAL) of the Polybot directive defensive guardrails campaign. Per-user EMA-smoothed bankroll multiplier scales the candidate's base suggested_size_usdc inside _build_trade_signal BEFORE engine.execute. signal_scan_job gains module-level _bankroll_ema_baseline dict + _bankroll_multiplier helper (multi-layered fail-safe: non-positive/non-finite balance returns 1.0; no prior baseline seeds + returns 1.0; raw multiplier clamped to [MIN, MAX]; EMA updated AFTER computing multiplier so the call uses prior baseline) + _bankroll_reset_for_tests. _build_trade_signal apply path lazy-imports get_settings; on BANKROLL_DYNAMIC_SIZING_ENABLED=true scales _proposed_size = base * Decimal(mult) quantized to cents (ROUND_DOWN); on any Exception falls back to base with logger.warning("bankroll_dynamic_sizing_failed", ...). 4 new config knobs (BANKROLL_DYNAMIC_SIZING_ENABLED=false DEFAULT — ships off, BANKROLL_MULTIPLIER_MIN=0.5, BANKROLL_MULTIPLIER_MAX=1.5, BANKROLL_EMA_ALPHA=0.05) + 2 field_validators (multiplier bounds reject non-finite + non-positive; alpha rejects non-finite + outside (0, 1]). Risk gate (Kelly + max_position_pct + per-trade ceiling) stays authoritative — multiplier only scales the input. Operator escape hatches (multiple layers): ENABLED=false (kill switch, instant revert), MIN=MAX=1.0 (runtime no-op), zero/non-finite balance, config-read exception. In-process memory state (Fly restart re-seeds at next scan → multiplier=1.0 transient, same trade-off as Lane 4). 34 hermetic tests (10 helper math: first-seed/growth/drawdown/upper-cap/lower-floor/non-positive-safe/non-finite-safe/EMA-drift/per-user-isolation/same-balance-returns-1; 12 config validators: defaults + parametrized non-finite+zero rejection on both bounds + alpha range rejection + alpha=1 inclusive accept; 5 behavioural: disabled-unchanged/enabled-first-seed/growth-scales-15/drawdown-scales-5/zero-balance-fallback) + 207 neighbor regression pass (Lane 1+2+3+4 tests + test_late_entry_v3 + test_signal_scan_job + test_flip_hunter_stale_price_fix + test_config_defaults); py_compile clean on both modified production files. Bug caught during regression sweep: _build_trade_signal is module-level scope, so the per-call `log = logger.bind(...)` of _process_candidate doesn't exist there — fixed to use module-level `logger.warning(...)`. Activation guards / paper-default invariant / per-user kill switch / multi-tenant isolation / 13-step risk gate untouched. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/bankroll-dynamic-sizing.md.
  - WARP/R00T/safe-close-direction-limit — STANDARD, NARROW INTEGRATION. Lane 4/5 of the Polybot directive defensive guardrails campaign. MERGED #1478 (2026-05-30) + DEPLOYED. Per-user / per-side rolling 1h counter caps accepted safe_close entries at config.SAFE_CLOSE_DIRECTION_LIMIT_PER_HOUR (default 8). Defends against the trending-market case where late_entry_v3's dynamic `fav_side = "YES" if yes_ask > no_ask else "NO"` filter leans the same direction across many candles — aggregating directional risk that the per-candle filter never sees (Polybot research: BTC downtrend Dec 14-18 2025 produced 55.9% NO win-rate = sample-period bias, not edge). signal_scan_job module-level state (_SAFE_CLOSE_DIRECTION_WINDOW_SEC=3600, _safe_close_direction_log keyed on (user_id, side.upper())) + 3 helpers (_safe_close_recent_count with O(n) in-place pruning, _safe_close_record_entry, _safe_close_reset_for_tests). _process_candidate step 3d gates (scoped to row["active_preset"]=="safe_close") before _build_trade_signal so we skip the signal build too. Record path inside the accept branch fires only on result.mode != "duplicate" — duplicate results never reach broker so they don't contribute to directional exposure. Other presets (close_sweep / flip_hunter / signal_following / copy_trade) bypass cleanly. In-process memory — Fly restart soft-resets (worst case: 8 entries × ~$10-$50 = bounded extra exposure, vs alternative of DB hit per scan tick on 15s cadence × many users × many candidates which is expensive). Operator escape hatch: SAFE_CLOSE_DIRECTION_LIMIT_PER_HOUR=0 disables without redeploy (runtime branches on `> 0`); negative values rejected at config load with ValidationError. 16 hermetic tests (5 window helpers covering empty/record/case-insensitive/prune/exact-boundary; 4 config knob default/env/disable/negative; 7 behavioural integration covering at-limit/under-limit/per-side-isolation/per-preset-bypass/disable-passes/record-on-accept/no-record-on-duplicate) + 188 neighbor regression pass (Lane 1+2+3 tests + test_late_entry_v3 + test_signal_scan_job + test_flip_hunter_stale_price_fix + test_config_defaults); py_compile clean on both modified production files. Risk gate / activation guards / paper-default invariant / per-user kill switch / multi-tenant isolation untouched. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/safe-close-direction-limit.md.
  - WARP/R00T/complete-set-edge-metric — MINOR, FOUNDATION. Lane 3/5 of the Polybot directive defensive guardrails campaign. MERGED #1477 (2026-05-30) + DEPLOYED. late_entry_v3._evaluate_market computes `complete_set_edge = round(1.0 - (yes_ask + no_ask), 4)` and stamps it into every SignalCandidate.metadata between existing `spread` and `seconds_to_close` keys. Polymarket binary UP/DOWN settles to $1.00 at expiry, so edge > 0 = textbook taker-side arb exists at quoted TOB (rare; book depth usually kills the actual fill); edge = 0 = efficient pricing; edge < 0 = overpriced relative to settlement bound (taker can never profit from full both-leg buy). Data was implicitly available via existing `spread` field but consumers had to recompute `1 - spread` — explicit naming + rounding (same IEEE-754 precision argument as Lane 2) eliminates that duplication and matches Polybot directive vocabulary. Strictly FOUNDATION — no trade logic change, no risk gate change, no entry filter change. Operator dashboards / aggregate alerts / future hard-gate lane can read the field once production data shows whether the signal is predictive. 9 hermetic tests (2 source pins on formula+stamp, 5 parametrized math fingerprint across zero/positive/negative regimes, 3 behavioural validating accepted-candidate metadata contains the metric in all 3 regimes within MAX_SPREAD=1.05); 176 neighbor regression pass (Lane 1+2 tests + test_late_entry_v3 + test_signal_scan_job + test_flip_hunter_stale_price_fix); py_compile clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/complete-set-edge-metric.md.
  - WARP/R00T/close-sweep-spread-gate — STANDARD, NARROW INTEGRATION. Lane 2/5 of the Polybot directive defensive guardrails campaign. MERGED #1476 (2026-05-30) + DEPLOYED. Per-leg bid-ask spread guard for close_sweep — the preset that fires in the final ~35s before candle close where book depth thins and a wide per-side spread = high slippage on a taker fill. New _best_bid helper in late_entry_v3 (mirror of _best_ask, scans + takes highest positive bid — no extra network call, bids are in the same /book payload). _evaluate_market gains max_leg_spread parameter; when set (> 0), rejects with reason="leg_spread_too_wide" if either (yes_ask-yes_bid) or (no_ask-no_bid) exceeds threshold, or "leg_spread_missing_bid" if a bid is absent (no-buyer scenario in the close window = even worse fill risk). _resolve_preset_params close_sweep dict injects max_leg_spread via getattr(cfg, "CLOSE_SWEEP_MAX_LEG_SPREAD", 0.02) (defensive against partial-mock cfg objects in tests); safe_close + flip_hunter omit the key → call sites pass None → gate no-ops (zero behaviour change for those presets, validates PR #1415 Kreo-parity untouched). Both scan call sites — run_close_sweep_fast + run_once Phase B2 — forward max_leg_spread. New config CLOSE_SWEEP_MAX_LEG_SPREAD: float = 0.02 with non-negative field_validator (same trap as TOB_STALE_MS: runtime branches on `> 0` so negative would silently disable). Metadata stamped with yes_bid/no_bid/leg_spread_yes/leg_spread_no for observability. 16 hermetic tests (3 _best_bid correctness, 4 source pins on signature/gate/preset-wire/call-site-forwarding, 3 config knob default/env/negative, 6 behavioural rejecting+accepting+disable+missing-bid) + 160 neighbor regression pass (Lane 1 test_tob_freshness_gate + test_late_entry_v3 + test_signal_scan_job + test_flip_hunter_stale_price_fix + test_config_defaults); py_compile clean on all 3 modified production files. Risk gate / activation guards / paper-default invariant / force-exit timing untouched. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/close-sweep-spread-gate.md.
  - WARP/R00T/tob-freshness-gate — MAJOR, NARROW INTEGRATION. Lane 1/5 of the Polybot directive defensive guardrails campaign (WARP•R00T synthesis of CRUSADERBOT_IMPROVEMENT_DIRECTIVES.md). MERGED #1475 (2026-05-30) + DEPLOYED. Defense layer over the existing sub-cent / Gamma-fallback guard (PR #1413). late_entry_v3._evaluate_market stamps metadata["entry_price_ts"] = datetime.now(timezone.utc).timestamp() at the point entry_price is finalized. services/signal_scan/signal_scan_job._process_candidate gains step 3b-0 between metadata→_live_fill_price resolution and the existing sub-cent guard: when (now - entry_price_ts) > config.TOB_STALE_MS (default 2000ms — Polybot research reference value), the candidate is rejected with scan_outcome="skipped_stale_tob" and telemetry.record_skip. Gate is scoped to candidates that carry the stamp — signal_following / momentum / copy_trade (no stamp) bypass cleanly, so the new layer cannot break non-candle strategies. Operator escape hatch: TOB_STALE_MS=0 disables without redeploy (gate branches on `_tob_stale_ms > 0`). 17 hermetic tests (4 source pins on the guard + stamp + scope + knob read; 2 config knob default/env-override; 1 disable-sentinel source pin; 10 parametrized boundary math tests over 0–60000ms) + 147 neighbor regression pass (test_flip_hunter_stale_price_fix + test_late_entry_v3 + test_signal_scan_job + test_config_defaults); py_compile clean on all 4 modified files. Risk gate / activation guards / paper-default invariant / force-exit timing untouched. Directive items explicitly DECLINED in this lane (advisory only — fundamental conflict with current arch): dual-leg simultaneous YES+NO entry, per-market inventory tracker (positions table already owns this), AUTO_CASH wallet RPC poll (custodial bot — internal ledger is authoritative), Flip Hunter Mode B (would revert PR #1415 Kreo-parity). WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/tob-freshness-gate.md.
  - WARP/ROOT/admin-user-detail-edit — MAJOR, NARROW INTEGRATION. Ops Console gains per-user detail + edit. Backend: GET /admin/users/{user_id} (AdminUserDetail — read-only superset of the AdminUsers row + wallet + open_positions + all editable settings), PATCH /admin/users/{user_id} (AdminUserUpdate — every field optional, validates each against the same allowed-value sets as user-facing endpoints: active_preset → _PRESET_TO_STRATEGY, risk_profile → _VALID_RISK_PROFILES{conservative,balanced,aggressive,custom}, capital_alloc_pct/tp_pct/sl_pct numeric bounds, max_per_trade_mode → {auto,fixed,pct}, max_per_trade_usdc [1,500], max_per_trade_pct [0.005,0.10]). Upsert ON CONFLICT preserves the paper-default invariant: brand-new user_settings rows get explicit 'paper' / 'balanced' / 0.40 literals via dynamic column-drop logic (no duplicate-column risk when patch already touches risk_profile/capital_alloc_pct). Audit-logged with actor admin user_id, target user_id, full patch payload. 400 on invalid UUID / empty patch / out-of-bounds; 404 on missing target. Frontend: AdminUserDetail + AdminUserPatch types in api.ts; new components/AdminUserDrawer.tsx (modal with read-only stat tiles + select/number form for every editable field, dirty-tracked draft, inline save+revalidate); AdminPage user rows now keyboard-accessible clickable (role="button" + Enter/Space + aria-label) opening the drawer, onSaved inline-syncs the row so the table reflects the edit without /admin/users reload. 17 new hermetic tests (detail happy-path, 404, 400 invalid UUID, telegram.local email strip, missing settings → paper/balanced default; patch validates preset/risk_profile/numeric-bounds×6 parametrized/empty-body/404-target/audit+detail-return/paper-default-invariant-on-minimal-patch); full admin_console suite 47/47 pass; ruff + py_compile + tsc + vite build clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/admin-user-detail-edit.md.
  - WARP/ROOT-max-trade-per-user — MAJOR, NARROW INTEGRATION. Per-user "Max trade setting" now caps signal_following (default strategy) sizing, not just late_entry_v3. signal_evaluator._resolve_size_usdc previously capped only by balance×capital_alloc_pct and ignored max_per_trade_mode/usdc/pct → a user's explicit "max $X/Y% per trade" was silently ineffective on the default strategy. Fix applies resolve_per_trade_ceiling as an extra min() cap for EXPLICIT fixed/pct modes only ('auto' unchanged — no $25 candle default imposed = no regression). PLUS Polymarket integration audit (code-as-truth vs official CLOB conventions): L1/L2 auth + CTF redeem + empty-book guards SOLID; top pre-LIVE finding = order path (live.py:222,397 + lifecycle.py:678) calls post_order without tick_size/neg_risk (get_tick_size/get_neg_risk exist, zero callers) → CLOB rejects non-tick-aligned prices + neg-risk markets need a different Exchange — recommended as the next lane (LIVE-gated, needs staging). Also flagged: legacy EOA-default _build_clob_client footgun + get_usdc_balance unit heuristic (left untouched pending verification). 5 new tests + 195 signal regression pass; ruff + py_compile clean. WARP•R00T self-validated directly. Report: projects/polymarket/crusaderbot/reports/forge/max-trade-per-user.md.
  - WARP/ROOT-live-path-hardening — MAJOR, NARROW INTEGRATION. Lane 5/5 (final) of the WARP•R00T pre-public audit fix campaign. All LIVE-gated (EXECUTION_PATH_VALIDATED off → no paper impact). L1: new integrations/polygon.nonce_lock(address) serializes nonce-read→sign→broadcast across all 4 on-chain sends (polygon_usdc.transfer_usdc/_send_native_matic/sweep_usdc_to_master + polymarket.submit_live_redemption), now reading with the 'pending' block tag — kills the master-EOA nonce race. L2: submit_live_redemption gains the INSTANT_REDEEM_GAS_GWEI_MAX gas ceiling (parity with transfer/sweep). L3: _load_enrolled_users now SELECTs live_capital_cap_usdc (was omitted → gate step 15 rejected every live trade from the auto-scan path). L4a: _passes_live_guards also requires USE_REAL_CLOB (mirrors assert_live_guards). L4b: gate Kelly validation bound 0.5→0.25 (matches constants + hardening + CLAUDE.md). 6 new tests + 89 withdrawal/sweep/custody/live-gate/redeem regression pass; ruff + py_compile clean. WARP•R00T self-validated. ADVISORY (not changed): MAX_CONCURRENT_TRADES=5 dead vs per-profile cap — WARP🔹CMD policy call. Report: projects/polymarket/crusaderbot/reports/forge/live-path-hardening.md.
  - WARP/ROOT-tg-callback-routing — STANDARD, NARROW INTEGRATION. Lane 4/5 of the WARP•R00T pre-public audit fix campaign. Closes B6: MVP _dashboard_cb/_settings_cb (attached first, group 0) won over legacy dashboard_nav_cb/settings_callback for ^dashboard:/^settings: and bounced every sub-route they didn't own to the MVP home → settings:tpsl/wallet/health/admin/redeem_set/profile/referrals/capital and dashboard:insights/portfolio/trades/wallet/auto/stop/monitor were silently dead. Fix (delegate-unknowns, no handler reorder): _settings_cb unknown branch now calls legacy settings.settings_callback (keeps MVP home/mode/risk/notifications/account/advanced/copy_wallet); _dashboard_cb keeps dashboard:main/refresh on MVP home and delegates the rest to legacy dashboard.dashboard_nav_cb. Lazy imports avoid circular refs; delegate runs before send_or_edit so legacy answers once. 6 functional routing tests + 61 existing dashboard/settings/ux tests pass; ruff + py_compile clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/tg-callback-routing.md.
  - WARP/ROOT-sse-token-exchange — MAJOR, NARROW INTEGRATION. Lane 3b/5 of the WARP•R00T pre-public audit fix campaign. Closes B2: the main 24h JWT rode in the SSE EventSource URL (?token=) → leaked into proxy/access logs (24h account credential). Fix: new authenticated POST /api/web/stream-token mints a 60s scope='sse' token (auth.mint_stream_token); GET /api/web/stream now validates only that scoped token (auth.decode_stream_token, enforces scope=='sse') instead of the main JWT; get_current_user REJECTS scope=='sse' tokens (401) so a leaked stream token can't call the API; frontend lib/sse.ts connect() is async — POSTs /stream-token (Bearer) then opens EventSource with the 60s token, fresh per (re)connect. 7 new tests + 22 auth regression pass; ruff + py_compile + tsc + vite clean. WARP•R00T self-validated. Suggested Next: WARP•SENTINEL (auth surface). Report: projects/polymarket/crusaderbot/reports/forge/sse-token-exchange.md.
  - WARP/ROOT-hero-risk-tag-crash-fix — STANDARD, NARROW INTEGRATION. Production SEV-1 hotfix: deployed WebTrader HOME crashed in the ErrorBoundary ("Cannot read properties of undefined (reading 'bg')"). Root cause: HeroCard RiskTag did RISK_COLOR[risk] (map only has safe/balanced/aggressive); DashboardPage passes data.risk_profile cast to RiskLevel, and 'custom' is a real profile → RISK_COLOR['custom'] undefined → c.bg threw → whole HOME screen down for custom-profile + auto_trade_on users. Fix: RiskTag falls back to RISK_COLOR.balanced + label RISK_LABEL[risk] ?? uppercased risk, so any unmapped value renders neutrally instead of crashing. Sibling .bg lookups (AlertCenter/NotificationPrefsCard) iterate fixed lists — not affected. 2 source-pin tests; tsc + vite build clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/hero-risk-tag-crash-fix.md.
  - WARP/ROOT-api-hardening — MAJOR, NARROW INTEGRATION. Lane 3a/5 of the WARP•R00T pre-public audit fix campaign. B1: GET /ops rendered the full ops dashboard (user counts, kill state, audit tail, health) to any anonymous visitor when OPS_SECRET unset → now 503 disabled notice, no data. B3: POST /ops/kill + /ops/resume accepted OPS_SECRET via legacy ?token= query param (secret-in-URL log leak → unauthenticated global kill switch) → token param removed from both mutators + _authorize_mutation; header (X-Ops-Token) or cookie only; GET /ops ?token=→cookie login migration preserved. B4: /autotrade/customize stored tp_pct/sl_pct/capital_alloc_pct/max_position_pct/max_per_trade_* with no validation → added bounds (tp(0,10], sl(0,1], capital_alloc(0,0.80], max_position(0,0.10], max_per_trade_usdc>0, max_per_trade_pct(0,1]). B5: /auth/register (5/min) + /auth/login (10/min) now IP-rate-limited via new per_ip_rate_limit (IP-keyed sibling of per_user_rate_limit, shared bounded buckets). 13 new tests + 2 existing ops tests updated to the new B3 contract; 82 ops/rate-limit/multitenant tests pass; ruff + py_compile clean. WARP•R00T self-validated. MERGED #1455. Report: projects/polymarket/crusaderbot/reports/forge/api-hardening.md.
  - WARP/ROOT-silent-broken-features — MAJOR, NARROW INTEGRATION. Lane 2/5 of the WARP•R00T pre-public audit fix campaign. Closes audit Theme #2 (silently swallowed failures hiding broken features). (1) copy-trade monitor built signal_ts with naive datetime.utcnow() → gate step 9 (datetime.now(timezone.utc) - signal_ts) raised TypeError caught at monitor.py:321 → EVERY copy-trade candidate silently died at the gate; now datetime.now(timezone.utc). (2) withdrawal approve/reject notifications passed parse_mode= to notify_user_by_telegram_id which rejected it (TypeError caught) → users never notified; added parse_mode param (default HTML) forwarded to send(). (3) WebTrader error-vs-empty: SettingsPage.load() now try/caught with error+Retry (was infinite spinner on getSettings/getDashboard failure); PortfolioPage AnalyticsPanel distinguishes fetch-error (error+Retry) from genuinely-empty; DashboardPage secondary loaders console.warn instead of fully silent. 4 hermetic tests; ruff + py_compile + tsc + vite clean. WARP•R00T self-validated. MERGED #1454. Report: projects/polymarket/crusaderbot/reports/forge/silent-broken-features.md.
  - WARP/ROOT-mode-status-display — STANDARD, NARROW INTEGRATION. Lane 1/5 of the WARP•R00T pre-public audit fix campaign. Closes audit Theme #1 (mode/status indicators read phantom DB keys) — the residual of the same class fixed earlier in mvp/dashboard.py. Telegram: mvp/settings.py read phantom live_mode_enabled/live_trading_enabled (always PAPER) → now reads canonical user_settings.trading_mode via _users.fetch_settings (SELECT extended); mvp/autotrade.py + mvp/onboarding.py read phantom auto_trade_enabled (always STOPPED) → canonical users.auto_trade_on. WebTrader: TopBar defaulted tradingMode to literal "paper" and 5 pages render <TopBar/> with no prop → LIVE users saw PAPER pill; added app-wide TradingModeContext fed from GET /me (extended to return trading_mode) + refreshed on system SSE, TopBar falls back to it; SettingsPage Activation-Guards row now reflects liveStatus.operator_guards_open (was hardcoded LOCKED). 5 source-pin regression tests pass; ruff + py_compile + tsc + vite build clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/mode-status-display.md.
  - WARP/ROOT-portfolio-status-alert-fixes — MAJOR, NARROW INTEGRATION. 3 owner-reported bugs: (1) exit_watcher mislabelled resolution-driven closes as tp_hit/sl_hit when the live mark gapped to a settlement extreme (≈0/≈1) — e.g. NO entry 0.69 closing +217% shown as "Take Profit (TP)"; now labels resolution_win/resolution_loss (new ExitReason members + alert_user_resolution_win/loss; threshold _RESOLUTION_PRICE_EPS=0.02). Genuine intraday TP/SL unchanged. (2) bot/handlers/mvp/dashboard.py read phantom key auto_trade_enabled → always "🔴 Stopped"; now reads auto_trade_on → "🟢 Running". (3) WebTrader alerts now sorted newest-first (App.tsx). 8 new tests; suite 2026 pass; ruff + tsc + vite clean. Report: reports/forge/portfolio-status-alert-fixes.md.
  - WARP/ROOT-admin-page — MAJOR, FULL RUNTIME INTEGRATION. WebTrader Ops console (`/admin`, role='admin' only). Overview: master hot-pool address + on-chain USDC/MATIC balance (funding target), 6 activation guards state, kill switch, system counts, last scan. Users roster (mode/balance/auto/open positions). Global strategy on/off (12 strategies) via new `strategies` table (migration 067, applied to Supabase, deny-by-default RLS) — wired FAIL-SAFE into the scanner at two points: `_preset_allows()` (per-tick-refreshed `_GLOBALLY_DISABLED_STRATEGIES` cache, covers lib+domain) and the signal_following loader (`NOT EXISTS`); missing row / DB error = strategy ON (no behaviour change). New endpoints `/api/web/admin/{overview,users,strategies}` + `/admin/strategies/toggle` gated by `_require_admin` (users.role='admin' → 403). `/me` now returns role+is_admin; Settings shows "🛡 Open Ops Console" for admins. 11 hermetic tests; full suite 2018 pass; ruff + py_compile + tsc + vite build clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/admin-page.md.
  - WARP/ROOT-account-link-telegram — MAJOR, FULL RUNTIME INTEGRATION. Reverse Telegram-link: an email-first WebTrader user unifies into ONE account across both surfaces (one user_id → LIVE/PAPER + wallet + trades always in sync). Closes the identity-asymmetry gap (Telegram→email link_email existed; email-first→Telegram created a 2nd unsynced account). One-time code flow: WebTrader Settings → Link Telegram mints a short-lived code (account_link_codes, migration 065); user redeems via bot /link <code>. domain/activation/account_link.py: no-duplicate → attach tg id to canonical (OK_LINKED); fresh duplicate (paper, zero positions/orders/deposits/withdrawals) → TOMBSTONE dup (synthetic unreachable email + users.merged_into, NOT deleted — several user FKs lack ON DELETE CASCADE) + reassign tg id (OK_MERGED); duplicate with trading history / live mode → BLOCKED → operator-assisted merge (no trades silently orphaned). New WebTrader endpoints /account/link-telegram/{status,start} (start rate-limited 5/min); bot /link handler; optional config TELEGRAM_BOT_USERNAME for a tap-to-open deep link; WebTrader "Link Telegram" Settings control. 15 hermetic tests; full suite 2005 pass; ruff + py_compile + tsc + vite build clean. WARP•R00T self-validated. NEEDS migration 065 applied to Supabase. Report: projects/polymarket/crusaderbot/reports/forge/account-link-telegram.md.
  - WARP/ROOT-live-activation-ux — MERGED #1438. MAJOR, FULL RUNTIME INTEGRATION. Completes the per-user LIVE opt-in UX symmetrically across WebTrader (full control) + Telegram (simple control). (1) Shared source of truth in domain/activation/live_opt_in_gate.py: LIVE_ENABLE_CONFIRM_PHRASE + LIVE_CAP_MIN/MAX_USDC (0 / 10000) + validate_live_capital_cap(); WebTrader router imports phrase+bounds (kills duplicated magic string); api.ts mirrors the constants. (2) Telegram CRITICAL fix: /enable_live now has a cap-capture step (warning → CONFIRM → enter cap 1-10000 → typed YES) and writes trading_mode='live' AND live_capital_cap_usdc together (was mode-only → every live trade rejected live_not_opted_in at gate step 15); new symmetric /disable_live single-step revert (cap preserved). (3) WebTrader LiveActivationModal: multi-step (readiness/gate preview → cap input → typed confirm phrase) wired to /live/{status,enable,disable}; described "Live Trading" section in Settings shows mode + cap + open exposure + switch-back-to-paper; mode-aware footer. (4) No-silent-failure hardening: write_mode_change_event returns bool (callers surface soft warning on audit-write failure); dashboard.py last-trade fallback logs instead of swallowing. Plain-language explanations on every step both surfaces. Full suite 1990 pass (added 31 tests, updated 2 flow pins); ruff + py_compile + tsc + vite build clean. WARP•R00T self-validated. Report: projects/polymarket/crusaderbot/reports/forge/live-activation-ux.md.
  - WARP/ROOT-public-readiness-audit — Axis #7 MAJOR FOUNDATION (read-only synthesis). Closes the public-ready sequence. WARP•R00T audit across 8 axes: (1) hard rules (no secrets / no threading / no silent except / no phase folders / Kelly 0.25 / 5 activation guards default FALSE), (2) multi-tenant safety (per-user kill+resume+emergency-stop; 7 cost-sensitive POSTs per-user rate-limited; SQL writes scoped by JWT user_id), (3) live-activation defence-in-depth (4-layer chain: operator env guards → per-user opt-in switch → 8-gate checklist → step 15 capital cap), (4) fake-price protections (metadata-first entry + sub-cent candle guard + live-mark TP/SL exits), (5) observability (DSN-gated Sentry + 4-dep healthcheck + structlog with format_exc_info), (6) rate-limit posture (600 RPM + /legal exempt + per-user buckets), (7) known-issues sweep (no public-launch blockers; only DB_POOL_MAX 48h Sentry watch as low-severity operational item), (8) migrations + tests (064 latest, applied to Supabase; 1959/1959 pass). VERDICT: APPROVED 95/100, 0 critical, ENGINEERING LIVE-READY. Going LIVE remains a WARP🔹CMD operational decision per state/LIVE_READINESS.md. Report: `projects/polymarket/crusaderbot/reports/forge/public-readiness-audit.md`. MAJOR, FOUNDATION.
  - WARP/ROOT-live-activation-flow — Axis #3 MAJOR backend foundations. Per-user live opt-in: migration 064 adds `user_settings.live_capital_cap_usdc NUMERIC(12,6) NOT NULL DEFAULT 0 CHECK (>= 0)` (applied to Supabase). Gate step 15 enforces the cap on every approved live trade — fires only when `chosen_mode == "live"`, rejects `live_not_opted_in` when cap=0 (defends against any path that sets trading_mode='live' without the typed-confirm flow), rejects `live_capital_cap_exceeded` when `(open_live_size + proposed_size) > cap` (aggregate exposure check; `_open_live_exposure` SUM helper fails closed on DB error). 3 new WebTrader endpoints: `GET /api/web/live/status` returns the per-user readiness snapshot (trading_mode + cap + open exposure + operator-guards state + 8-gate `live_checklist` outcome); `POST /api/web/live/enable` flips `trading_mode='live'` only after operator env guards open + 8-gate checklist passes + body has the EXACT confirm phrase `"ENABLE LIVE TRADING FOR MY ACCOUNT"` (case-sensitive, no normalisation) + `0 < cap <= 10_000`; `POST /api/web/live/disable` is a single-step revert. All three wrap `per_user_rate_limit("live_activation", limit=5/min)` and write `audit.write` rows on state change. `GateContext` + `TradeSignal` + `_build_gate_context` + `_build_trade_signal` plumbed for the cap field. 14 hermetic tests (gate step 15 source pins + logic correctness + endpoint pins + confirm-phrase constant pin); 1959/1959 full suite pass; py_compile clean. Defence-in-depth chain on a live trade now: operator env guards → per-user opt-in switch → 8-gate checklist (at opt-in) → step 15 capital cap (per trade). MAJOR, NARROW INTEGRATION. WARP•R00T self-validated APPROVED 91/100, 0 critical. Report: `projects/polymarket/crusaderbot/reports/forge/live-activation-flow.md`.
  - WARP/ROOT-realtime-fill-price — WARP🔹CMD escalation, MAJOR. Two surgical changes to kill the remaining "fake price" sources after the flip-hunter sub-cent fix: (1) `signal_scan_job._process_candidate` prefers `cand.metadata["entry_price"]` (real CLOB /book best-ask captured at strategy scan time) over re-fetching via `get_live_market_price` for `late_entry_v3` candidates — guarantees the fill price is the same real-orderbook value the gate evaluated; non-candle strategies (signal_following etc.) still use the helper fallback. (2) `exit_watcher.evaluate` returns the live mark (`cur`) as `current_price` on TP_HIT / SL_HIT instead of the synthetic `_tp_exit_price` / `_sl_exit_price` formula — the synthetic was the visible "5 trades on BTC/ETH/SOL/XRP/DOGE all closing at 0.581" symptom WARP🔹CMD called out. Synthetic preserved as fallback only when `cur` is None (unreachable in production but kept for defensive depth). 5 hermetic tests including a "distinct markets must produce distinct exits" regression pin that fails closed if the synthetic comes back. 4 existing tests updated to the new contract (2 exit_watcher TP/SL assertions, 2 band-gate tests refocused from "scan-to-fill drift" — which can no longer happen — to "candidate metadata band violation"). Full local suite 1945/1945 pass. py_compile clean. WARP•R00T self-validated APPROVED 92/100, 0 critical. MAJOR, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/realtime-fill-price.md`.
  - WARP/ROOT-flip-hunter-stale-price-fix — Bug-fix lane (urgent). WARP🔹CMD-reported screenshot pair showed 5 flip_hunter trades on BTC/ETH/SOL/XRP/DOGE all entering @ 50.5¢ and exiting @ 63.1¢ for identical +$0.50 P&L on the same 5m candle window — impossible on real markets. DB query confirmed 72/86 flip_hunter positions @ exactly 0.505 + 8 @ 0.515, both off the Polymarket 0.01 CLOB tick. **Root cause**: `integrations.polymarket.get_live_market_price` (`polymarket.py:414-440`) fell through to Gamma `outcomePrices` when CLOB `/price` returned the empty-book sentinel; fresh 5m candle markets opened with Gamma seed `["0.505","0.495"]` before any real CLOB activity → bot traded that as the live mark. Combined with the intentional synthetic TP-fill (`exit_watcher._tp_exit_price = entry × (1 + tp%)` for paper-mode polling-gap protection) every TP-hit on the seed price landed on the same 0.58075 across coins. **Fix** in two layers: (1) `get_live_market_price` rejects Gamma fallback values not on the 0.01 tick (`abs(price*100 - round(price*100)) > 1e-6`); (2) `_process_candidate` in signal_scan_job adds a belt-and-suspenders tick check that records `scan_outcome="skipped_sub_cent_price"` so the operator panel surfaces reject rate. 15 hermetic regression tests (YES + NO sub-cent reject, tick-aligned accept, real CLOB short-circuits Gamma, source-level pin on the consumer guard, parameterised tick-detector correctness). **DB cleanup** at `scripts/cleanup_flip_hunter_sub_cent_2026_05_28.sql`: single transaction reverses fake realised P&L via `T_ADJUSTMENT` ledger entries (User A −$17.32 / User B −$12.84), voids the 3 still-open sub-cent positions at entry price (zero P&L, `exit_reason='cleanup_void'`), refunds their size_usdc, writes an audit_log row, and `RAISE EXCEPTION`-asserts that no affected open position remains. `mode='paper'` filter on every clause; gated `BEGIN ... ROLLBACK` until WARP🔹CMD explicitly authorises COMMIT (cleanup will run via Supabase MCP on ack). WARP•R00T self-validated APPROVED 93/100, 0 critical. MAJOR, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/flip-hunter-stale-price-fix.md`.
  - WARP/ROOT-multitenant-safety — Axis #1 MAJOR. WARP•R00T self-validated under WARP🔹CMD delegation; verdict APPROVED 91/100, 0 critical. **Patch A (CRITICAL fix)**: `/api/web/kill` + `/api/web/emergency-stop` were activating the GLOBAL `system_settings.kill_switch_active` — any authenticated WebTrader user could halt every other user's trading. Both endpoints now call `users.set_paused(user_id, TRUE)` (risk gate honours `ctx.paused` at gate.py:259). New `/api/web/resume` clears the per-user flag. `RuntimeStatus` schema gains `user_paused`; `EmergencyStopResponse.kill_switch_active` → `user_paused`. Frontend `api.ts` types mirrored; new `api.bot.postResume`. **Patch B (HIGH fix)**: new `api/per_user_rate_limit.py` — sliding-window in-memory bucket keyed on `(user_id, scope)`, 50 k-key cap + idle-eviction, 429 + `Retry-After` on overflow. Applied to `/wallet/withdraw` (10/min), `/copy-trade/tasks` (20/min), `/positions/{id}/redeem|close` (30/min), `/kill` + `/resume` shared `user_pause` scope (10/min), `/emergency-stop` (5/min). Complements the global per-IP limiter (600/min). **Patch C**: `tests/test_multitenant_safety.py` 9 hermetic tests, including source-level pins that fail closed if a regression re-introduces `kill_switch.set_active` in `web_kill`/`web_emergency_stop` or drops the `per_user_rate_limit` decorator from any of the 7 protected endpoints. Regression neighbourhood (test_rate_limit + test_health + test_warp54_closed_beta_hardening) 54/54 pass. The global kill switch is now reachable only from operator paths (`/api/ops/kill` cookie+token gated; Telegram `/kill` operator-chat-id gated). MAJOR, NARROW INTEGRATION. Reports: `projects/polymarket/crusaderbot/reports/forge/multitenant-safety.md` + `projects/polymarket/crusaderbot/reports/sentinel/multitenant-safety.md`.
  - WARP/ROOT-onboarding-ux — Axis #4 public-launch legal surface. New mode-neutral Terms of Service at `projects/polymarket/crusaderbot/legal/terms-of-service.md` (~120 lines: trading-aid framing, no profit guarantee, responsible-use clause, paper+live both described, standard liability/availability/governing-law). privacy-policy.md moved from repo root into the bot tree at `projects/polymarket/crusaderbot/legal/` so it ships in the Docker build context. New `api/legal.py` FastAPI router with `GET /legal/{terms,privacy}` — reads the .md files and renders them as minimal HTML (escape-first, then re-introduce a tight Markdown subset: H1-H3, lists, links restricted to http(s)/relative, bold/italic/inline-code); `<meta name="robots" content="noindex">`. `/legal/*` exempted in the rate-limit middleware so a pre-signup user reading the ToS is never throttled. AuthPage gains a footer line under the existing "Secure channel" indicator: "By continuing you agree to our **Terms of Service** and **Privacy Policy**. CrusaderBot is a trading aid, not financial advice — no profit is guaranteed." Both links open in new tabs. test_rate_limit + test_health (48 tests) still pass; py_compile clean. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/onboarding-ux.md.
  - WARP/ROOT-observability-ops — Axis #5 M-3 structlog migration. Top-8 stdlib `logging` modules rewritten to `structlog` keyword-arg API across 129 call sites: users.py (10), domain/risk/kill_switch_exec.py (11), integrations/polymarket.py (16), bot/handlers/admin.py (16), domain/strategy/strategies/late_entry_v3.py (16), scheduler.py (22), domain/execution/lifecycle.py (21), domain/execution/exit_watcher.py (17). monitoring/logging.py processor chain extended with `structlog.processors.format_exc_info` so `log.exception()` serialises tracebacks correctly. Multi-line `%s` formatters flattened into structured kwargs; `exc_info=True` inside `except` converted to `log.exception(...)`; `extra={}` dicts inlined. Test `test_log_resumed_open_positions_emits_count` migrated from `caplog` (which can't see structlog's filtering bound logger) to `structlog.testing.capture_logs()`. Caught + fixed mid-PR: `event` is a reserved structlog kwarg (the message itself) — `event=event` → `payload=event` in lifecycle.handle_ws_fill drop logs (commit 0c766f3). Sentry + healthcheck audit: monitoring/sentry.py DSN-gated, FastAPI + Starlette integrations wired, never crashes boot; api/health.py /health runs check_database + check_telegram + check_alchemy_rpc + check_alchemy_ws (full WS handshake) — no hardening needed. Deferred: structlog → stdlib bridge so Sentry breadcrumbs capture WARN/ERROR from migrated paths (unhandled exceptions still captured via FastAPI integration). Full local suite 1912 passed / 6 skipped / 0 failures. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/observability-ops.md. MERGED #1428 (chunk 1) + #1429 (chunk 2); this lane closes the axis with state-sync only.
  - WARP/ROOT-notif-followups — Axis #6 notification follow-ups. In-process TTL dedup cache (30s) keyed on (user_id, alert_key, dedup_key=market_id) inside `notification_prefs.route_outgoing_alert()` (with `_evict_stale_dedup` and `_clear_dedup_cache` helpers). All trade-lifecycle alerts (position_opened/closed, copy_trade_executed, exit_watcher exit alerts) propagate `market_id` as `dedup_key` so a duplicate fire on the same (user, alert_key, market) inside the window is silently suppressed. New `monitoring/alerts.alert_user_low_balance()` with per-user 1h cooldown keyed on `("low_balance", str(telegram_user_id))`. Scheduler registers hourly `check_balance_alerts` job; threshold via `config.LOW_BALANCE_THRESHOLD_USDC` (0 disables). auth_events instrumentation dropped (no user value, scope expansion). 3 new dedup tests; 14/14 notification_prefs tests pass. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/notif-followups.md. MERGED #1427.
  - WARP/ROOT/notifications-overhaul — WebTrader AlertCenter visual + interaction refresh to match the Kreo-style notifications mockup the owner shared. Header gains a "Mark all read" action (dismisses every visible alert in one shot + bumps lastSeen; idempotent, localStorage-quota-safe). Dismiss × promoted from hover-only (`opacity-0 group-hover:opacity-100`) to always-visible for tap-device discoverability. Body text wraps (`whitespace-pre-line break-words`) instead of one-line truncate so long market titles like "BUY signal on 'Will Bitcoin exceed $120k?'" stay readable. Footer gains a `⚙ Preferences` link that routes to /settings (closes the panel first so the destination is visible). Category surface re-balanced: TRADE / SIGNAL (📡, merged with the prior COPY category) / RISK / SYSTEM — matches the three example cards in the screenshot. App.tsx `AlertCenterCtx` gains `markAllRead: () => void`; default context provides a no-op so existing consumers never crash. Backend / SSE / alert fetch path untouched — only client-side state changes. tsc --noEmit clean; vite build clean (4.33s; bundle sizes within rounding). No tests added (no pre-existing AlertCenter tests). MINOR, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/notifications-overhaul.md`. PR open.
  - WARP/ROOT/safe-close-flip-hunter-kreo-parity — Realign safe_close + flip_hunter candle presets to Kreo's reference behaviour (per Kreo Polymarket Telegram bot screenshots). Three structural changes scoped strictly to late_entry_v3: (1) safe_close — min_ask_diff 0.08→0.01 (Kreo Min Edge 1%) + new force-exit at rem=30s so the position closes BEFORE the noisy final 30s where SL was firing at random (pre-fix data: 95% of safe_close trades landed outside the [0.60, 0.70) intended band; ~50% were sl_hit at coin-flip prices). (2) flip_hunter — inverted from late-window underdog (entered cheap side 0.26-0.36 in last 140s rem) to early-window favored side (Kreo "With Trend": enters first 140s elapsed for 5m / 420s for 15m, favored side 0.50-0.95, Min Edge 3% / ask_diff ≥0.03); new force-exit at end of entry window (rem=160s for 5m, rem=480s for 15m). (3) Kreo-style fixed-time exit MECHANISM: late_entry_v3.evaluate_exit rewritten as two-stage — checks force-exit (seconds_to_close ≤ force_exit_at_rem_sec) BEFORE flip-stop. New `force_exit_at_rem_sec_for(preset, timeframe)` helper is the single source of truth for the (preset, tf) → rem threshold map; consumed by both the entry path (via _resolve_preset_params → scan metadata) and the exit path (via exit_watcher's payload enrichment). signal_scan_job._resolve_preset_params(preset, tf) is the new per-tf-aware resolver (flip_hunter is the only tf-variant); replaces the duplicated inline param-dict builds in both run_close_sweep_fast and run_once Phase B2. OpenPositionForExit gains active_preset + selected_timeframe (pulled from existing user_settings JOIN in list_open_for_exit — no migration); exit_watcher.registry_strategy_evaluator enriches the evaluate_exit payload with these + seconds_to_close (computed from resolution_at, no extra DB/API call) + force_exit_at_rem_sec, only for late_entry_v3. close_sweep unchanged (already matches Kreo Close Sweep). 27 new hermetic tests (13 late_entry_v3 — metadata, evaluate_exit branches, helper lookup; 8 signal_scan_job — resolver correctness across both timeframes; 6 exit_watcher — end-to-end fire/hold for safe_close 30s, close_sweep no-force-exit invariant, flip_hunter 5m+15m, missing resolution_at fallback to flip-stop-only). Full suite 1894 pass; ruff + py_compile clean. Risk gate (13 steps) + activation guards untouched. MAJOR, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/safe-close-flip-hunter-kreo-parity.md`. MERGED #1415 + DEPLOYED 2026-05-28 ~07:13 UTC. Operator-side: post-deploy verify entry_price distribution sits inside intended band for ≥95% of new rows AND positions close at the configured force-exit threshold (safe_close ~rem 30s, flip_hunter 5m ~rem 160s, flip_hunter 15m ~rem 480s).
  - WARP/ROOT/late-entry-fill-drift-guard — Fill-time price-band re-check for late_entry_v3 candidates (close_sweep / safe_close / flip_hunter). Root cause: candidates passed scan-time gates with one set of orderbook prices, then `_process_candidate` fetched live fill via `get_live_market_price` and built TradeSignal with no re-validation. Candle markets oscillate 0.10+ in seconds; prod 24h showed only 5.5% of safe_close trades + 17% of close_sweep trades inside intended band (avg entry 0.45, fills 0.04–0.94). Fix: `late_entry_v3._evaluate_market` now emits `fav_price_min` + `fav_price_max` in candidate metadata; new gate 3c in `_process_candidate` (between live-price fetch 3b and signal build) requires `_live_fill_price ∈ [fav_price_min, fav_price_max)` — `skipped_fill_drifted` otherwise. Metadata-driven and opt-in: candidates without the band keys (signal_following / lib / confluence_scalper) pass through unchanged. Risk gate (13 steps) + activation guards untouched. 6 new hermetic tests (4 gate-3c branches + 2 strategy-metadata regression); full suite 1866 pass; ruff + py_compile clean. STANDARD, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/late-entry-fill-drift-guard.md`. MERGED #1414 + DEPLOYED 2026-05-28 ~05:48 UTC.
  - WARP/ROOT/readme-warp-readiness-update — README refresh post WARP•R00T LIVE+PAPER readiness pass. Added `Engineering: Live Ready` (green) badge alongside existing Paper Beta / Paper Only badges; new "Operating Posture" section explicitly framing "production PAPER ONLY / engineering LIVE-ready" with the five activation-guard names (`ENABLE_LIVE_TRADING`, `EXECUTION_PATH_VALIDATED`, `CAPITAL_MODE_CONFIRMED`, `RISK_CONTROLS_VALIDATED`, `SECURITY_HARDENING_VALIDATED`) called out + link to `state/LIVE_READINESS.md` for owner-operational final go-live sequence; WARP•R00T added to System Architecture diagram + Authority Chain table (note: assigned explicitly; FORGE remains default); Branch Naming section documents both `WARP/{feature}` (FORGE/SENTINEL/ECHO) and `WARP/ROOT/{feature}` (R00T) + explicit ban on auto-generated `claude/*` branches. Doc-only — no code touched. MINOR. MERGED #1412.
  - WARP/ROOT/live-readiness-final — Lane 3/3 of WARP•R00T LIVE+PAPER readiness pass. State-doc sync only (no code). Updated `state/LIVE_READINESS.md` with Lane 1 audit verdict + Lane 2 paper-default-hardening closure; `state/PRODUCTION_CHECKLIST.md` section A gained an explicit "PAPER-default invariant hardened (#1410)" checkbox; `state/WORKTODO.md` Active section records the three lanes. The owner-only "Final go-live sequence" in LIVE_READINESS.md is preserved verbatim — every remaining step is operational (fund master USDC+MATIC, apply prod migrations, flip the activation guards in order). MINOR, state sync. Report: `projects/polymarket/crusaderbot/reports/forge/live-readiness-final.md`. MERGED #1411.
  - WARP/ROOT/paper-default-hardening — Lane 2/3 of WARP•R00T LIVE+PAPER readiness pass. Belt-and-suspenders enforcement of the PAPER-default-for-new-users invariant: every `INSERT INTO user_settings` in production code now writes `trading_mode='paper'` explicitly instead of relying solely on the schema column default (migration 001:73). Two write sites in `users.py` (Telegram new-user `upsert_user`, lazy-create in `get_settings_for`) and one new write site in `webtrader/backend/auth.py` (email-signup parity with Telegram). F-LOW-1 silent `except Exception: pass` in webtrader signup replaced with `logger.exception(...)` (CLAUDE.md HARD RULE no-silent-failures). New hermetic suite `tests/test_paper_default_invariant.py` (5 tests): functional INSERT-call-shape checks for upsert_user + get_settings_for; source-level regex guards on `users.py` + `auth.py` that fail-closed if any future edit drops the literal `trading_mode='paper'` from a user_settings INSERT; no-silent-pass guard. PAPER remains the only mode any user gets at creation regardless of `ENABLE_LIVE_TRADING`. The LIVE-flip path (8-gate `live_checklist.evaluate()` + typed CONFIRM) is unchanged. Targeted regression 10/10 pass; full suite 1859 passed / 1 skipped / 0 failures; ruff + py_compile clean. STANDARD, NARROW INTEGRATION. Report: `projects/polymarket/crusaderbot/reports/forge/paper-default-hardening.md`. PR open. Next: Lane 3 `WARP/ROOT/live-readiness-final` (state sync only).
  - WARP/ROOT/system-ready-audit — Lane 1/3 of WARP•R00T LIVE+PAPER readiness pass. Read-only audit. Verdict: system engineering-ready, 0 current risks. Verified all 5 LIVE activation guards default false (`config.py:174-182` + `fly.toml:24-28`), the assert_live_guards chain (`domain/execution/live.py:50-80`) checks 8 conditions including `USE_REAL_CLOB`, `role=='admin'`, `trading_mode=='live'`; execution router (`domain/execution/router.py`) audit-logs `GUARD_BYPASS_ATTEMPT` at CRITICAL and paper-falls-back on any guard failure; LIVE-flip is hard-gated by `live_checklist.evaluate()` (8 gates) + typed CONFIRM + defense-in-depth re-check; PAPER-default for new users is schema-enforced (`migrations/001_init.sql:73`) and protected across all three creation paths (Telegram `upsert_user`, WebTrader `auth.signup_email`, lazy `get_settings_for`); on-chain capital paths #1402/#1403/#1408 all triple-gated OFF. Surfaced 3 brittleness items for Lane 2 (F-MEDIUM-1 schema-only paper-default reliance; F-LOW-1 silent `pass` in webtrader signup bootstrap; F-LOW-2 no regression test pinning paper-stickiness when ENABLE_LIVE_TRADING=True). MINOR, FOUNDATION. Report: `projects/polymarket/crusaderbot/reports/forge/system-ready-audit.md`. MERGED #1409.
  - WARP/ROOT/safe-custody-cutover — Chunks 3+4/4 of custody migration; final code lane. wallet/custody.py dispatcher routes transfer_usdc + sweep_usdc_to_master to polygon_usdc (EOA, default) or new SafeCustody class (gasless via Polymarket relayer) based on CUSTODY_MODE. SafeCustody.transfer_usdc signs master Safe → destination via RelayClient.execute([SafeTransaction]); SafeCustody.sweep_usdc_to_master signs user Safe → master Safe; sync SDK wrapped in asyncio.to_thread so event loop never blocks. Triple-gated for Safe mode: EXECUTION_PATH_VALIDATED + CUSTODY_MODE='safe' + is_relayer_configured(); _ensure_safe_mode_wired raises BuilderRelayerUnavailable on misconfig (no silent EOA fallback). withdrawals._attempt_onchain_transfer + scheduler._sweep_deposits_onchain now import from wallet.custody; PreflightError re-exported so the typed error surface stays uniform. Default CUSTODY_MODE='eoa' keeps every call on the merged C1 (#1402) + sweep (#1403) paths — PAPER untouched. 13 hermetic tests (dispatcher routing, all guards, balance pre-flight, dust skip, no silent fallback); full suite 1860 passed; ruff + py_compile clean. WARP•SENTINEL APPROVED 94/100, 0 critical. MAJOR, NARROW INTEGRATION. Reports: reports/forge/safe-custody-cutover.md + reports/sentinel/safe-custody-cutover.md. PR open. Operator-side go-live: acquire Builder creds (polymarket.com/settings?tab=builder), set fly secrets, flip USE_BUILDER_RELAYER then CUSTODY_MODE='safe' for a small cohort.
  - WARP/ROOT/deposit-watcher-safe-aware — Chunk 2b/4 custody migration. scheduler.watch_deposits SQL extended to SELECT safe_address; new pure helper _build_watched_addresses(rows) returns (addresses, addr_by_lower) with BOTH EOA + Safe addresses routing to the same user_id (Safe added only when not NULL, so pre-backfill wallets stay EOA-only with no regression). Confirmation/reorg state machine (mig 047) untouched. Polymarket Magic-link / Safe-flow deposits now credit the correct user without any custody-mode flip. 3 hermetic helper tests; full suite 1847 passed; ruff + py_compile clean. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/deposit-watcher-safe-aware.md. PR open. Next: 3 SafeCustody capital paths (needs Builder creds), 4 cutover.
  - WARP/ROOT/safe-address-precompute — Chunk 2/4 of custody migration. wallets.safe_address column (migration 061, additive + partial-unique index); wallet/safe.py with deterministic local CREATE2 derivation via RelayClient.get_expected_safe() (NO builder creds needed — pure local math), inline set_safe_address_in_conn writer called from vault.create_wallet_for_user, and idempotent backfill_safe_addresses for existing rows. SafeDerivationUnavailable typed error keeps SDK absence recoverable. EOA custody behavior unchanged — column is informational until Chunk 4 cutover. 8 hermetic tests; full suite 1844 passed; ruff + py_compile clean. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/safe-address-precompute.md. PR open. Backfill is not auto-invoked — call manually post-deploy. Next: 2b deposit-watcher Safe awareness, 3 SafeCustody capital paths (needs Builder creds), 4 cutover.
  - WARP/ROOT/builder-relayer-foundation — Chunk 1/4 of the gasless / Safe-proxy custody migration. New integrations/builder_relayer.py wraps the Polymarket Builder relayer SDK (py-builder-relayer-client + py-builder-signing-sdk added to requirements); soft-imports + typed BuilderRelayerUnavailable + is_relayer_configured()/build_builder_config()/make_relayer_client(signer_pk)/relayer_sdk_importable() factory surface. Gated by 4 secrets-default-None (POLY_BUILDER_API_KEY/SECRET/PASSPHRASE) + USE_BUILDER_RELAYER toggle (default False) + CUSTODY_MODE='eoa' (default). Dormant — no active path consumes it yet; PAPER + the shipped capital paths (#1402, #1403) unchanged. 9 hermetic tests; full suite 1836 passed; ruff + py_compile clean. STANDARD, FOUNDATION. Report: projects/polymarket/crusaderbot/reports/forge/builder-relayer-foundation.md. PR open. Next chunks (need Builder creds from polymarket.com/settings?tab=builder): 2 Safe deploy + safe_address column, 3 SafeCustody capital paths, 4 staged cutover.
  - WARP/ROOT/live-readiness-sync — readiness-doc truth sync (MINOR). Closed drift: LIVE_READINESS.md + PRODUCTION_CHECKLIST.md now reflect both capital paths complete (withdraw #1402, sweep #1403) and H1/H2/H3/M1/M3 hardening; added the authoritative owner-only "Final go-live sequence" to LIVE_READINESS.md. Confirmed zero remaining NotImplementedError/stub in trading/risk/execution/redeem/withdraw/sweep (only non-core lib/strategies/weather_arb.py TODO remains). No code change. Report: projects/polymarket/crusaderbot/reports/forge/live-readiness-sync.md.
  - WARP/ROOT/deposit-sweep-onchain — on-chain deposit sweep (hot-pool consolidation). integrations/polygon_usdc.py:sweep_usdc_to_master() reads a user EOA deposit wallet's full USDC balance, tops it up with MATIC from the master (deposit wallets hold no native token) via _send_native_matic(), then signs the USDC transfer to the master pool with the wallet's own key; status==1 asserted. scheduler._sweep_deposits_onchain() runs per-user sequentially (cron max_instances=1 → no nonce race), marks deposits swept only after a confirmed tx, isolates per-user failures. Double-gated behind EXECUTION_PATH_VALIDATED + SWEEP_ONCHAIN_ENABLED (both default OFF) — paper keeps the logical-only sweep. Polymarket gasless relayer not used (needs Safe/Proxy custody + Builder creds; documented as future-native optimization). Config: SWEEP_ONCHAIN_ENABLED/MIN_USDC/GAS_TOPUP_MATIC. No migration (existing columns). 4 new tests; full suite 1827 passed; ruff + py_compile clean. WARP•SENTINEL APPROVED 94/100, 0 critical. MAJOR, NARROW INTEGRATION. Reports: reports/forge/deposit-sweep-onchain.md + reports/sentinel/deposit-sweep-onchain.md. PR open.
  - WARP/ROOT/withdrawal-onchain-path — C1: on-chain withdrawal capital path wired. New integrations/polygon_usdc.py:transfer_usdc() signs+broadcasts an ERC-20 USDC transfer from the master hot-pool (mirrors submit_live_redemption signing); hard-gated behind EXECUTION_PATH_VALIDATED, with pre-flight gas-price ceiling (INSTANT_REDEEM_GAS_GWEI_MAX), hot-pool USDC + MATIC balance checks, status==1 receipt assertion. wallet/withdrawals.py:_attempt_onchain_transfer() now drives the live lifecycle: paper = deferred no-op (row untouched); live = processing → transfer → completed + tx_hash, or failed + onchain_error; idempotent (existing tx_hash never re-sent; approve gates on status='pending'). Migration 060 adds tx_hash (partial-unique) + onchain_error. 5 new hermetic tests; full suite 1820 passed; ruff + py_compile clean. Replaces the NotImplementedError skeleton from #1375. MAJOR, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/withdrawal-onchain-path.md. PR open — SENTINEL gate required before merge.
  - WARP/ROOT/ops-auth-cookie — H1: ops-auth hardening. Secret out of the URL: POST /ops/login sets an HttpOnly+Secure+SameSite=Lax cookie holding an HMAC of OPS_SECRET; GET /ops renders login form when unauthenticated (no data leak) and dashboard once authed; kill/resume authenticate via cookie OR X-Ops-Token header OR legacy ?token= (backward-compatible — no kill-switch lockout); legacy ?token= GET auto-migrates to a cookie + clean redirect. Login body parsed via parse_qs (no python-multipart boot dependency). Same-origin CSRF check on cookie mutator path (_is_same_origin; header/token paths exempt). All secrets.compare_digest. test_api_ops.py 48 passed; full suite 1808 passed; ruff clean. MAJOR, NARROW INTEGRATION. MERGED #1400. Report: projects/polymarket/crusaderbot/reports/forge/ops-auth-cookie.md.
  - WARP/ROOT/alchemy-ws-handshake — M3: check_alchemy_ws (monitoring/health.py) rewritten from TCP-only probe to a full WebSocket handshake + eth_blockNumber JSON-RPC round-trip (mirrors check_alchemy_rpc); /health now fails on a broken WS, not just closed TCP. Soft websockets import (matches integrations/clob/ws.py); _with_timeout still scrubs URL/secret to class-name-only. 4 new hermetic tests; tests/test_health.py 42 passed; ruff + py_compile clean. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/alchemy-ws-handshake.md. PR open.
  - WARP/ROOT/api-rate-limit — H2 inbound HTTP rate limiting. New RateLimitMiddleware (api/rate_limit.py): per-client (Fly-Client-IP / XFF / peer) sliding-window limiter, outermost middleware in main.py; RATE_LIMIT_ENABLED/RPM(120)/WINDOW(60) config knobs; 429 + Retry-After + structured WARN; asyncio-only; mem-bounded (10k clients, idle eviction); /health,/ready,/api/web/health,/telegram/webhook exempt. 6 new hermetic tests; full suite 1798 passed; ruff clean. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/api-rate-limit.md. PR open.
  - WARP/ROOT/public-ready-hardening — WARP•R00T public-ready audit. Verified paper-safe core (0 hardcoded secrets; ENABLE_LIVE_TRADING=False config.py:148 + fly.toml; CAPITAL_MODE_CONFIRMED/EXECUTION_PATH_VALIDATED False; Kelly clamped gate.py:382 cap 0.25; no threading; no silent except; outbound CLOB+Telegram throttling; RLS 42/43). Fixed H3: privacy-policy.md rewritten from stale Custom-GPT stub → real CrusaderBot policy. Declared lanes for SENTINEL: C1 on-chain capital wiring (wallet/withdrawals.py:158 NotImplementedError — LIVE blocker), H1 ops-auth token-out-of-URL (api/ops.py), H2 inbound rate limiting, M1 last RLS table, M3 alchemy WS handshake. Report: projects/polymarket/crusaderbot/reports/forge/public-ready-hardening.md. MAJOR, FOUNDATION. PR open.
  - WARP/ROOT/bot-html-to-markdownv2 — H3: migrated all 26 legacy bot/handlers from ParseMode.HTML to ParseMode.MARKDOWN_V2 (9 batches; settings.py, signal_following.py, admin.py, copy_trade.py plus 22 prior files). Removed html.escape(), added _md() for dynamic content, wrapped financial values in code spans, escaped MD2 special chars in static strings. Preserved ParseMode.HTML on live_checklist.render_telegram() call sites. STANDARD, NARROW INTEGRATION. Report: projects/polymarket/crusaderbot/reports/forge/bot-html-to-markdownv2.md. PR open.
  - WARP-KB-V2 KEYBOARD-V2-REDESIGN (branch WARP/keyboard-v2-redesign) PR open — migrated the entire Telegram keyboard layer from the legacy 3-generation bot/keyboards/ to the redesigned keyboards_v2/, then promoted keyboards_v2/ to the active bot/keyboards/ (legacy archived at bot/_keyboards_archive/). keyboards_v2 was gap-filled into a behavior-preserving SUPERSET of every keyboard function the live handlers use: v2 drop-ins where callback_data matched, verbatim ports (distinct names + aliased imports) where it diverged (p5:*, auto_trade:*, panel:*, ops:refresh/pause/lock, start:*, mytrades:*, close_position:*, position:fc_*) — so NO callback_data changed for any existing action. New 2-step preset risk-tier flow (preset:tiers/preset:tier:) folded into preset_callback, and emergency progressive disclosure (emergency:home/more/ask/status) folded into emergency_callback — no new dispatcher patterns required. The self-contained mvp/ tree + 4 kept modules (copy_trade/signal_following/market_card/referral) copied as-is (redesign deferred). All 24 handlers migrated (aliased renames only, no call-site logic changed); affected tests repointed to new locations/_keyboards_archive with 4 assertions updated to the redesigned tier/emergency/unified-nav behavior. dispatcher imports clean; 1751 tests pass (no regression); grep "from ..keyboards[^_]" bot/handlers/ = 0; grep keyboards_v2 = 0. Pre-existing latent bug noted (settings:back main_menu(strategy_key=...) TypeError) left unchanged. STANDARD, FULL RUNTIME INTEGRATION. Source: projects/polymarket/crusaderbot/reports/forge/keyboard-v2-redesign.md.
  - WARP-42 AUDIT-FINDINGS-FIX (Linear WARP-42, remediates WARP-41 BLUEPRINT-DRIFT-MAJOR): three lanes MERGED A→B→C. Lane A MERGED PR #1303 (WARP/CRUSADERBOT-BLUEPRINT-V3.3-SYNC) — docs/blueprint/crusaderbot.md v3.2→v3.3, edits A1–A13 (audit-log mig 033, copy_targets/copy_trade_tasks coexistence, RBAC+user_tiers note, §11 table renames/absent/added + count ~43, risk gate rewritten to 0–14 steps, scan-freq 5min, auto-redeem 200 gwei + same-tick dispatch, 3 presets added=11 total, activation-guard annotations); decision-items defaulted with <!-- DECISION-NEEDED --> markers (2FA deferred / fee 10% / roadmap dual-scheme / referrer_id unwritten). Lane B MERGED PR #1304 (WARP/CRUSADERBOT-CONFIG-GAPS-PATCH) — config.py SECURITY_HARDENING_VALIDATED guard added (default False, closes §12/M-5) + AUTO_REDEEM_ENABLED paper-intent comment. Lane C MERGED PR #1305 (WARP/CRUSADERBOT-DEPOSIT-REORG-GUARD, H6) — DEPOSIT_CONFIRMATION_DEPTH=32 + migration 047_deposit_confirmation_state.sql (additive/idempotent, backfills legacy rows to confirmed) + scheduler.watch_deposits rewritten to pending→confirmed (credit at depth)→reverted (un-credit on removed=true) state machine + integrations/polygon.py surfaces log removed flag; 4/4 new tests (tests/test_deposit_reorg.py) + existing test_sweep_deposits.py green. Prophylactic — guarded behind ENABLE_LIVE_TRADING=False. STANDARD, WARP🔹CMD review (no SENTINEL gate per task). State-file sync = this entry.
  - SYSTEM AUDIT 2026-05-23 (WARP/crusaderbot-system-audit): WARP•SENTINEL CORE AUDIT pre-client-handoff against main HEAD 9caaabc + blueprint v3.1. Verdict BLOCKED, Score 62/100, 1 critical. F-CRIT-1: bot/ui/__init__.py imports BAR/BRANCH/LAST/STATUS_*/PAPER/LIVE/LOCKED no longer defined in bot/ui/tree.py (rewritten WARP-67/68/71/73) → main.py:23→bot.dispatcher→MVP handlers→messages_mvp→ui.tree ImportError; app cannot start from main. Verified: direct import fails + test_activation_handlers FAIL + test_warp59 collection ERROR (same cause). F-HIGH-2: 0 positions/orders/fills/snapshots live despite 6 users (5 auto-on) — frontend renders empty. Safety core all PASS; realtime NOTIFY triggers wired (migs 029/030 live — stale [NOT STARTED] note); blueprint conformance high (BaseStrategy+registry+4 strategies, presets, instant/hourly redeem, fee/referral guards OFF); deliberate divergences access_tier→role + copy_targets→copy_trade_tasks. 1606 pytest pass, ruff clean, frontend build clean. Report: projects/polymarket/crusaderbot/reports/sentinel/crusaderbot-system-audit.md. MAJOR.
  - WARP-67 (issue #1284): Telegram UX final clean — Bug 1 box-drawing tree (`│ ├── └──`) replaced by flat Markdown: ui/tree.py helpers (title/leaf/section/nested/join_blocks) re-emit `*Header*` + `Key: Value` lines, new md_escape() escapes `_ * ` `` ` `` `[` in dynamic labels/values; _send.send_or_edit() parse_mode default None→"Markdown" (single switch, all MVP screens). Bug 2 main_menu_kb(configured=) → "🤖 Auto Mode" when preset configured even if Stopped. Bug 3 keyboards/mvp/autotrade.home_kb(paused=) → Pause/Resume/Start. Bug 4 menus/main Settings+Help → _group0_noop (legacy double-response removed). Bug 5 render_positions_list/render_markets_trending sanitize market title (strip + collapse newlines). messages_mvp BAR/BRANCH/LAST glyph lines removed. 2 tests realigned to new routing (test_phase5d, test_ux_overhaul). 1614 passed. PR open: WARP/warp67-ux-final-clean. STANDARD, NARROW INTEGRATION.
  - WARP-66 (issue #1282): Telegram UX full polish — dispatcher.py auto-mode keyboard regex widened to (Auto-Trade|Auto Trade|Auto Mode|Setup Auto) + new `▶️ Resume` group=-1 handler routing to MVP autotrade (do_resume/show_home); menus/main.py auto-mode routes → _group0_noop to suppress double response (orphaned `autotrade` import removed). autotrade.py show_home status ladder running→RUNNING/paused→PAUSED/configured→STOPPED/else→NOT_SET + _read_state `configured` field + strategy label from PRESET_CONFIG.name. copy_wallet.py show_home running→RUNNING/wallets→STOPPED/else→NOT_SET. onboarding.py returning user re-armed with persistent main_menu_kb before show_dashboard. messages_mvp render_dashboard_default `🤖 Auto Trade`→`🔄 Auto Trade` (dedupe Bot Status emoji). 1614 passed. PR open: WARP/warp66-ux-polish. STANDARD, FUNCTIONAL.
  - SENTINEL RE-AUDIT 2026-05-22 (issue #1276 / WARP/sentinel-reaudit-2026-05-22): CORE AUDIT re-run post WARP-62+63 fix cycle. Verdict APPROVED, Score 96/100, 0 critical. F-001 (H2 threading) FIXED — registry.py eager `_DEFAULT_REGISTRY` at line 182, no `import threading`. F-002 (copy_targets drift) CLEARED — domain/signal/copy_trade.py reads canonical copy_trade_tasks (wallet_address/status='active'/copy_amount). All H1–H8 PASS, zero hard-rule breach. Full suite 1613 passed / 1 skipped / 0 failures (incl. 9 WARP-63 hermetic + 36 confluence scalper + WebTrader exposure). Residual P2 (non-blocking): dual CopyTradeStrategy classes + orphaned copy_targets writes from legacy /copytrade wizard — deferred cleanup lane. Live Fly.io /health UNVERIFIED (network allowlist blocks fly.dev); endpoint code intact. Prior BLOCKED verdict LIFTED. Report: projects/polymarket/crusaderbot/reports/sentinel/reaudit-2026-05-22.md. MAJOR.
  - WARP-65 (issue #1278): Telegram UX fix — main_menu_kb() → ReplyKeyboardMarkup (persistent bottom nav, auto_on/paused/open_count params); send_or_edit routes ReplyKeyboard via reply_text; dashboard STATUS_STOPPED added; strategy label from PRESET_CONFIG.name; open_count via fetch_open_position_count; do_start() sends keyboard on launch. 1613 passed. PR open: WARP/warp65-telegram-ux-fix. STANDARD, NARROW INTEGRATION.
  - WARP-64 (issue #1277): CI pytest fix — 4 failing tests in test_warp59_copy_wallet_bridge.py resolved: wrong patch target (_send.send_or_edit → copy_wallet.send_or_edit), missing effective_message on mock update, queue undercount in bridge e2e test. MERGED #1279. STANDARD, MECHANICAL.
  - WARP-62 + WARP-63 (issues #1273 + #1274): threading removed from registry.py (eager module-level init, no import threading); domain/signal/copy_trade.py migrated from copy_targets to copy_trade_tasks (wallet_address, copy_amount, last_seen_tx removed). MERGED #1275. STANDARD, MECHANICAL.
  - WARP-51 MERGED (1b9c3fdb5e6c): every `.py` writer/reader of access_tier stripped; `set_tier`/`force_set_tier` deleted; `/allowlist` → `set_role('admin')`; `scripts/seed_operator_tier.py` deleted + `fly.toml [deploy].release_command` removed; migration `044_drop_access_tier.sql` re-enabled (IF EXISTS, idempotent); 16 test fixtures swept; 1487 pytest passed. MAJOR, NARROW INTEGRATION. SENTINEL APPROVED 99/100.
  - WARP-43 RUNTIME-TRADE-SMOKE (WARP/runtime-trade-smoke, PR open): scan_runs telemetry table (migration 048) + ScanTelemetry dataclass + structured log events (strategies_loaded, scan_input, strategy_run, risk_gate, paper_execution) + startup loud-failure RuntimeError guard + GET /admin/scan/last + GET /admin/scan/list. 5 files modified/created; py_compile clean. STANDARD, NARROW INTEGRATION.

[IN PROGRESS]
- WARP/ROOT/closed-count-fix: MERGED (#1392) — WebTrader CLOSED/ALL badge fix — PortfolioSummary.total_closed (DB COUNT), router query, api.ts interface, PortfolioPage totalClosed computed from summary?.total_closed.
- WARP/R00T-webtrader-wallet: MERGED (#1373) — WebTrader withdraw UI — POST /wallet/withdraw, WithdrawModal 3-step, api.requestWithdrawal, WalletPage wired. Report: projects/polymarket/crusaderbot/reports/forge/webtrader-wallet-withdraw.md.
- WARP/copy-task-repo-returning-fix: MERGED (#1374) — F-HIGH-2 root cause — create_task/update_task RETURNING missing nickname/copy_direction/execution_mode/allow_topups → KeyError → copy task creation always silently failed. Fixed both RETURNING clauses. Report: projects/polymarket/crusaderbot/reports/forge/copy-task-repo-returning-fix.md.
- WARP/withdraw-onchain-skeleton: MERGED (#1375) — on-chain withdraw skeleton — _attempt_onchain_transfer() behind EXECUTION_PATH_VALIDATED guard; paper deferred, live raises NotImplementedError until polygon_usdc wired. Report: projects/polymarket/crusaderbot/reports/forge/withdraw-onchain-skeleton.md. SUPERSEDED by WARP/ROOT/withdrawal-onchain-path (C1 — NotImplementedError now replaced with real transfer_usdc wiring; pending SENTINEL).
- WARP/R00T-copy-targets-migration: MERGED (#1372) — WARP-57 MEDIUM-4 — copy_targets table retired. Migration 058 (backfill + DROP), all 3 legacy write sites migrated to copy_trade_tasks. Report: projects/polymarket/crusaderbot/reports/forge/copy-targets-migration.md.
- WARP/R00T-wallet-deposit-withdraw: MERGED + DEPLOYED. Migration 057 applied. Paper withdraw flow + deposit UX live. Report: projects/polymarket/crusaderbot/reports/forge/wallet-deposit-withdraw.md.
- WARP/R00T-sse-session-pooler-listen: MERGED + DEPLOYED — SSE LISTEN reconnect-loop fixed. 5 tests, 1774 pass. Report: projects/polymarket/crusaderbot/reports/forge/sse-session-pooler-listen.md.
- WARP/R00T-mock-clob-parity: MERGED + DEPLOYED — MockClobClient get_usdc_balance() stub + SENTINEL MAJOR live execution path audit (CONDITIONAL 84/100, 0 critical) + all 5 activation guards enforced + Kelly assert→raise. Report: reports/sentinel/live-execution-path.md.
- 48h observation window started 2026-05-26 16:00 WIB — TooManyConnectionsError confirmed silent (0 events last 1h). Ends 2026-05-28 16:00.
- Production monitoring of late_entry_v3 on Fly (paper): FAV_PRICE_MAX=0.70 ceiling live. Watch 24-48h net PnL.

[NOT STARTED]
- Wire share_trade_kb into trade close call sites when PNL > 0; surface ready, wiring deferred.
- Referral payout activation: separate lane, requires WARP🔹CMD decision.
- Fee collection activation: separate lane, requires WARP🔹CMD decision.
- Wire @require_access_tier('PREMIUM') onto trading command handlers as separate lane.
- Seed boss user ADMIN tier row in user_tiers via /admin settier post-deploy.

[NEXT PRIORITY]
- WARP🔹CMD review + merge of WARP/R00T/bankroll-circuit-breaker (MAJOR — Polybot directive 1.4 + Sprint-1 #6, NARROW INTEGRATION). Single signal_scan_job gate insertion (step 0a between crash-recovery and dedup) + 3 config knobs + 2 validators + 19 hermetic tests. No migration required. Default OFF (BANKROLL_CIRCUIT_BREAKER_ENABLED=false) so dark-launch is safe — no live behaviour change vs current. After merge: deploy with default OFF, soak 24-48h, then operator flips to true once trip-rate is reviewed. Active order cancellation half deferred to future live-mode lane. Report: projects/polymarket/crusaderbot/reports/forge/bankroll-circuit-breaker.md.
- [DONE — MERGED 16c0fae] WARP/R00T/complete-set-edge-gate (MAJOR — Polybot directive Sprint-1 #4+#5).
- [DONE — MERGED 29aaa88] WARP/R00T/admin-drawer-complete (STANDARD — admin Ops Console surface, NARROW INTEGRATION). Completes the admin User-Detail drawer (identity block, per-user pause toggle, timeframe + assets editor, recent trades + audit, TP% label fix). Backend additive (schemas + 1 router endpoint extended + 1 validator extended); frontend additive (1 component rewritten). No migration required. Smoke test after deploy: open the drawer on @walk3r69, flip pause, edit asset chips, save, confirm the dashboard SCANNER pill flips to PAUSED (Admin) and the audit.log row reflects the change. Report: projects/polymarket/crusaderbot/reports/forge/admin-drawer-complete.md.
- [DONE — MERGED de8f291] WARP/R00T/alert-center-typed-cards. End-to-end AlertCenter redesign closes the owner-reported "Telegram-dump in WebTrader" defect. Fly.io CD deploying. After deploy verify next trade-open / TP-hit cards render as typed (icon + market truncate + side pill + colored P&L).
- WARP🔹CMD review + merge of WARP/ROOT/admin-drawer-mobile-friendly (MINOR — NARROW INTEGRATION). Owner-reported mobile bug: AdminUserDrawer's Save/Cancel buttons hidden behind BottomNav (HOME/AUTO/PORT/WALLET/CONFIG) — overlay was z-50 vs BottomNav z-[100]. Fix: bumped overlay to z-[200] + 3-region modal layout (sticky header with close ✕ + flex-1 scrollable body + sticky footer with always-visible Cancel/Save) + max-h-[90vh] for mobile chrome breathing room + larger tap targets. Single-file CSS/DOM restructure, no behaviour change. tsc + vite build clean. Known issue (flagged for follow-up): WithdrawModal, LiveActivationModal, DepositModal all still use z-50 — same overlap bug but not in this lane's scope. Report: projects/polymarket/crusaderbot/reports/forge/admin-drawer-mobile-friendly.md.
- WARP🔹CMD review + merge of WARP/ROOT/admin-user-detail-edit (MAJOR — NARROW INTEGRATION). Tier MAJOR (touches user-settings persistence + admin-only mutation surface). WARP•SENTINEL validation recommended before merge per CLAUDE.md; otherwise WARP🔹CMD review on the diff (backend additive, frontend additive, schema additive, tests cover validation + audit + paper-default invariant). Report: projects/polymarket/crusaderbot/reports/forge/admin-user-detail-edit.md.
- [DONE — MERGED #1470 + #1471 + HEISENBERG_API_TOKEN live in Fly secrets] heisenberg-survey + admin-user-detail-edit follow-up review fixes + agent 585 confidence wire. All 6 Heisenberg agents active in prod (574/575/568/585/584/581); 585 social_momentum flag now drives ceiling-protected confidence boost (+0.05, capped at 0.90) inside `_run_heisenberg_signals` so position-sizing feels social pulse.
- [DONE — MERGED #1472 + DEPLOYED + ENABLED + VERIFIED] WARP/ROOT/heisenberg-556-realtime-trades. Agent 556 buffer live: 195 rows / 146 distinct wallets / sub-minute end-to-end latency confirmed via Supabase shape check. Raw JSONB contains id/side/outcome/condition_id/token_id/size/timestamp/proxy_wallet/transaction_hash — every field the existing copy_trade._process_one() reads.
- WARP🔹CMD review + merge of WARP/ROOT/copy-trade-realtime-fast-track (STANDARD — NARROW INTEGRATION). New consumer `services/copy_trade/realtime_fast_track.py` reads the heisenberg_realtime_trades buffer every 30s, groups active copy_trade_tasks by leader wallet, dispatches each fresh trade (by per-task watermark) to the EXISTING `monitor._process_one(task, raw_jsonb, wallet)` — no logic fork, dedup via existing `copy_trade_idempotency` table prevents double-fire with the slower wallet-watcher path. Migration 071 adds `copy_trade_tasks.last_realtime_seen_at TIMESTAMPTZ` (applied to Supabase prod). 2 new config knobs (HEISENBERG_FAST_TRACK_ENABLED default FALSE + HEISENBERG_FAST_TRACK_INTERVAL_SEC=30). Triple-gated: token + producer flag + consumer flag, plus all 4 existing copy-trade safety gates (kill_switch / globally_disabled / per-user pause / 13-step risk gate). 11 new hermetic tests (3 gate skips + 1 dispatch happy-path + watermark UPDATE + 1 cutoff filter + 1 NULL-watermark fallback + 1 per-task failure isolation + 1 fetch-failure never-raises + 3 source pins) + 1877 existing pass; py_compile clean. Net: leader trade → user mirror evaluation in ~60s instead of 5-10 min. Report: projects/polymarket/crusaderbot/reports/forge/copy-trade-realtime-fast-track.md.
- WARP🔹CMD review + merge of original heisenberg-556-realtime-trades NEXT PRIORITY (superseded above): client (`services/heisenberg_trades.py`) + scheduled job (`jobs/heisenberg_realtime_sync.py`) + buffer table (migration 070 — `heisenberg_realtime_trades` with dedup unique on `(wallet, condition_id, trade_time, side)`, wallet-time DESC index for consumer lookups, retention sweep index, deny-by-default RLS, named CHECK constraints on price∈[0,1] + size_usdc≥0). Triple-gated: HEISENBERG_API_TOKEN env + HEISENBERG_REALTIME_TRADES_ENABLED config (DEFAULT FALSE) + scheduler conditional add_job. Defensive field aliasing (`wallet`/`proxy_wallet`/`address`/`trader`, `side`/`direction`/`outcome`, `trade_time`/`timestamp`/`ts` ISO + epoch). UTC normalisation on both `_coerce_dt` branches. Top-level DB-acquire guard honours "never raises" contract. `_first_not_none` preserves legitimate falsy 0/0.0 prices/sizes through alias resolution. Buffer DORMANT — no consumer reads it yet; future follow-up lane wires copy_trade.monitor for sub-minute fast-track entry. 21 new hermetic tests (Gemini + CodeRabbit review applied: falsy-0 preservation, broadened never-raises, UTC normalise, DB-acquire guard, CHECK constraints, prune-parse warning) + 1851 existing pass; py_compile clean. NEEDS: migration 070 applied to Supabase, then HEISENBERG_REALTIME_TRADES_ENABLED=true flipped in Fly secrets after observing one tick's log shape for field-name confirmation. Report: projects/polymarket/crusaderbot/reports/forge/heisenberg-556-realtime-trades.md.
- WARP🔹CMD review + merge of WARP/R00T/strategy-system-cleanup (MAJOR — FULL RUNTIME INTEGRATION). Closes the operator strategy toggle contract end-to-end: (a) copy_trade.monitor now respects strategies.enabled (was bypassed → copy_trade fired even when admin OFF), (b) WebTrader + Telegram pickers fetch /autotrade/preset-availability and hide presets whose backing strategy is admin-OFF, (c) admin panel narrowed 12→3 strategies (late_entry_v3, signal_following, copy_trade) — 9 cosmetic toggles deleted because no preset routed to them. Lib/strategies/* (11 modules) + 2 dead domain strategies + multi-strategy presets (whale_mirror, hybrid, ensemble, full_auto, etc.) + _keyboards_archive/ + 9 dead test files archived. Migration 068 deletes the 9 cosmetic rows from `strategies`. 1817/1822 pass, 0 fail. Report: projects/polymarket/crusaderbot/reports/forge/strategy-system-cleanup.md. NEEDS: mig 068 applied to Supabase + operator visual check on /admin.
- WARP🔹CMD review + merge of WARP/ROOT/clob-tick-size (MAJOR). CLOB order correctness fix — all three post_order call sites (live.py:222,397 + lifecycle.py:678) now fetch and forward tick_size + neg_risk from MarketDataClient. LIVE-gated; no paper impact. 8/8 tests + 77/77 regression pass. Report: projects/polymarket/crusaderbot/reports/forge/clob-tick-size.md.
- WARP🔹CMD review + merge of WARP/ROOT-public-readiness-audit (final lane of the public-ready sequence). WARP•R00T self-validated APPROVED 95/100, 0 critical. Report: projects/polymarket/crusaderbot/reports/forge/public-readiness-audit.md.
- After merge: **PUBLIC-READY SEQUENCE COMPLETE**. Going LIVE is a WARP🔹CMD operational decision per state/LIVE_READINESS.md — fund master USDC+MATIC → set RISK_CONTROLS_VALIDATED → EXECUTION_PATH_VALIDATED → CAPITAL_MODE_CONFIRMED → ENABLE_LIVE_TRADING → small-cohort SWEEP_ONCHAIN_ENABLED.
- Apply migration 065 to Supabase (additive: users.merged_into + account_link_codes), then WARP🔹CMD review + merge of WARP/ROOT-account-link-telegram. WARP•R00T self-validated, 2005/2005 tests pass. Report: projects/polymarket/crusaderbot/reports/forge/account-link-telegram.md.
- [DONE — MERGED #1438] WebTrader LiveActivationModal + Telegram /enable_live cap step + /disable_live (per-user LIVE opt-in synced across both surfaces).
- Final go-live is OWNER-ONLY operational (see state/LIVE_READINESS.md "Final go-live sequence"): fund master USDC+MATIC → apply prod migrations (incl. 060 + 063) → RISK_CONTROLS_VALIDATED → EXECUTION_PATH_VALIDATED → CAPITAL_MODE_CONFIRMED → ENABLE_LIVE_TRADING → enable SWEEP_ONCHAIN_ENABLED for a small cohort. Keep withdrawal_approval_mode='manual' first cohort.

[KNOWN ISSUES]
- [RESOLVED — WARP/ROOT-settings-email-linked-state] WebTrader Settings re-showed the "Link Email" form after refresh even when an email was already linked (linked state was transient client-only). Fixed: /me returns persisted email/username/telegram_linked (tombstone @telegram.local emails excluded); Settings shows a persistent "Email — connected" row and hides the form when an email exists.
- [APPLIED 2026-05-28] Migrations 065 (users.merged_into + account_link_codes) + 066 (account_link_codes RLS deny-by-default) applied to Supabase via MCP. Security advisor: 0 rls_disabled_in_public (account_link_codes now in the rls_enabled_no_policy deny-by-default set with every other public table). TELEGRAM_BOT_USERNAME=CrusaderPolybot set in fly.toml (PR WARP/ROOT-account-link-rls-deeplink) for the WebTrader Link-Telegram deep link.
- [RESOLVED — WARP/ROOT-account-link-telegram #1439] Account linking was one-directional (Telegram→email only); an email-first WebTrader user using the bot created a 2nd unsynced account. Fixed: reverse /link flow unifies into one canonical account (tombstone fresh duplicate; block history-bearing duplicate for operator merge). Migrations 065+066 applied.
- [RESOLVED — WARP/ROOT-live-activation-ux #1438] Telegram /enable_live wrote trading_mode='live' without live_capital_cap_usdc → live trades silently rejected live_not_opted_in at gate step 15. Fixed: cap-capture step now writes both; WebTrader LiveActivationModal added; shared phrase/cap source of truth across all 3 surfaces.
- [MONITORING] DB_POOL_MAX lowered 10→3 (PR #1366, 2026-05-26). Supabase Session Pooler active. 48h window to confirm TooManyConnectionsError silent in Sentry.
- [ACTIVE] FLIP_STOP_PRICE lowered 0.48→0.10 (near-disabled) for close_sweep. Monitor if this causes positions to hold past resolution without closing — market_expired exit should catch it.
- [RESOLVED — RLS, verified 2026-05-27 via Supabase advisors + list_tables] Row Level Security ENABLED on ALL 43/43 public tables (migration 046 + later migs incl. 055 scan_runs). Security advisor returns 0 rls_disabled_in_public errors; remaining rls_enabled_no_policy INFO lints are the intended deny-by-default design (no anon policy). M1 closed.
- [RESOLVED — LANE-3 2026-05-24, branch claude/fervent-hawking-yNP0Z] Edge-model bug #1 FIXED: abs(yes_p-0.5) removed from jobs/market_signal_scanner.py; replaced with max(|oneDayPriceChange|, |oneHourPriceChange|×1.5) momentum scoring + price range tightened to 0.15–0.85 to reject extreme longshots. Side follows momentum direction. Report: projects/polymarket/crusaderbot/reports/forge/edge-model-momentum.md. The 20 aggressive-user positions already in flight are unaffected (they resolve naturally ~Jun–Jul 2026); new signals after deploy will not enter extreme-longshot futures.
- [RESOLVED — RLS, verified 2026-05-26] migration 046 (ENABLE RLS, no FORCE, no policies → deny-by-default for anon; postgres owner + service_role bypass; frontend via backend API/SSE only) applied and confirmed live: 42/43 public tables now RLS-enabled. Source: projects/polymarket/crusaderbot/reports/forge/rls-enable-anon-lockout.md.
- [RESOLVED — F-CRIT-1, in PR #1294] bot/ui/__init__.py re-export of BAR/BRANCH/LAST (box chars removed from bot/ui/tree.py by WARP-67/73) was breaking main.py→bot.dispatcher→MVP handler import chain. Fixed by removing the dead re-export (status/mode constants untouched). Re-verified: import clean, 1613 pytest pass, 0 collection errors, ruff clean. Forge report: projects/polymarket/crusaderbot/reports/forge/fix-bot-ui-tree-constants.md. Pending Fly redeploy.
- [HIGH — F-HIGH-2, root-caused 2026-05-23] 0 positions/orders/fills/portfolio_snapshots despite 5 auto-on users. PRIMARY CAUSE FOUND + FIXED in WARP/lib-strategy-load-fix: the lib/ auto-trade strategies (the candidate source for signal_scan_job.run_once) never loaded in prod — lib/ was outside the Docker build context (never shipped) AND the file-path loader could not resolve their package-relative imports (ImportError for every strategy). Now vendored into the package + importable + loader rewritten; all 7 strategies load. Live trade generation to confirm post-deploy. SECONDARY (still open): Phase C evaluate_publications_for_user may also yield zero candidates (6,964 publications, 0 trades) — separate follow-up.
- [RESOLVED — migration 059, 2026-05-27] Supabase function_search_path_mutable WARN ×9 cleared — all 9 cb_notify + positions trigger functions now have SET search_path = public, pg_catalog. Remaining deferred from same audit: WebTrader 690 kB single-chunk bundle; no frontend test framework.
- [DEFERRED — audit 2026-05-23] stale [NOT STARTED] note re migrations 029/030 (triggers confirmed live).
- [RESOLVED — WARP-62 / re-audit 2026-05-22] `domain/strategy/registry.py` `import threading` + `threading.Lock()` removed; eager module-level `_DEFAULT_REGISTRY = StrategyRegistry()` (line 182). H2 HARD RULE re-evaluated PASS. Verified in SENTINEL re-audit (issue #1276).
- [RESOLVED — WARP-63 / re-audit 2026-05-22] `domain/signal/copy_trade.py` `CopyTradeStrategy.scan()` now reads canonical `copy_trade_tasks` (wallet_address/status='active'/copy_amount); legacy `copy_targets` read + `last_seen_tx` write-back removed. Both scanner paths now on the canonical table. Verified in SENTINEL re-audit (issue #1276). Residual P2 (non-blocking): legacy `/copytrade` wizard + setup.py still WRITE `copy_targets` (orphaned, no reader) and a duplicate CopyTradeStrategy lives in `domain/strategy/strategies/copy_trade.py` — follow-up cleanup lane (delete dup class + DROP TABLE copy_targets + remove legacy write sites).
- [RESOLVED in WARP-52] portfolio_snapshots Python writer wired — paper.close_position fires write_snapshot inline after txn commit; scheduler runs portfolio_snapshots tick every 60s; cb_portfolio NOTIFY trigger now live via INSERT on portfolio_snapshots. Source: projects/polymarket/crusaderbot/reports/forge/portfolio-snapshots-writer.md. ledger-derived equity chart at services/portfolio_chart.py preserved in parallel — convergence is a separate lane.
- [RESOLVED in WARP-26] copy_trade.py now queries copy_trade_tasks — legacy copy_targets table is orphaned (schema cleanup deferred to a separate migration lane).
- qwneer8 and Maver1ch69 have identical bad tp_hit positions from price bug #1105 (same 0.540–0.545 inflated exit_price). Not cleaned in this lane — WARP🔹CMD decision required to extend cleanup.
- WARP🔹CMD requested removal of the internal Tier-4/activation-guard LIVE-trading safety gate; WARP•FORGE declined that sub-item only — CLAUDE.md forbids bypassing the live-trading guard. assert_live_guards is intentionally preserved (invisible to users; live remains owner-gated + OFF). All other role-model items delivered as requested.
- [RESOLVED in WARP-51] users.access_tier integer column scheduled for DROP on next Fly deploy via migration 044; user_tiers string-tier table retained (parallel system, separate lane).
- New users before runtime-autotrade-fix deploy still receive $0 balance — two affected users (qwneer8, Maver1ch69) already backfilled via SQL; fix ships with this PR.
- Fly.io deploy blocked until migration 030 (job_runs metadata JSONB) applied to production — trading-unblock MERGED PR #1065, live on main.
- crusaderbot-logo.png binary not yet in repo — WebTrader logo img references will render broken until PNG committed to webtrader/frontend/public/ by WARP🔹CMD.
- 5 positions stuck open — trading-unblock MERGED PR #1065. Will auto-close as MARKET_EXPIRED within 1 exit_watch tick (60s) after migration 030 applied and Fly.io deploy completes.
- fly CLI not installed in cloud execution environment — deploy step requires WARP🔹CMD manual execution from fly CLI machine.
- migration 027 (notifications_on) must be applied to production before deploying PR #1049 + PR #1055 code on Fly.io.
- pnl_insights.py, copy_trade.py, portfolio_chart.py still contain ━━━ — out-of-scope for crusaderbot-mvp-runtime-ux; separate cleanup lane required.
- WARP🔹CMD to verify Fly.io production deploy of PR #1049 + PR #1055 changes before activating on Fly.io.
- Keep production PAPER ONLY until explicit owner live activation decision.
- /deposit has no tier gate; intentional and non-blocking.
- check_alchemy_ws is TCP-only and does not perform a full WebSocket handshake; low-priority follow-up.
- ENABLE_LIVE_TRADING code default RESOLVED by crusaderbot-finalize MERGED PR #1075 — config.py default now False (paper-safe); fly.toml [env] still forces false; SENTINEL 96/100 sign-off complete.
- [DEFERRED] Ops auth full hardening (per-operator login, token rotation, token-out-of-URL) intentionally deferred for paper-mode beta. Mutators are timing-safe secret-gated, every flip audited with client_host breadcrumb, hardened bearer /admin/kill exists. Not an incomplete stub — documented in api/ops.py + config.py.
- [RESOLVED — WARP/ROOT/deposit-sweep-onchain] Nightly sweep on-chain consolidation now WIRED (sweep_usdc_to_master + _sweep_deposits_onchain, master-funded gas top-up). Double-gated behind EXECUTION_PATH_VALIDATED + SWEEP_ONCHAIN_ENABLED (both default OFF) — paper still runs the logical-only accounting sweep, no real capital moves. SENTINEL APPROVED 94/100.
- integrations/polymarket.py _build_clob_client() is dead in the live execution path after Phase 4B but still indirectly referenced by submit_live_redemption(); cleanup deferred to WARP/CRUSADERBOT-POLYMARKET-LEGACY-CLEANUP.
- Activation guards remain NOT SET and must not be changed without explicit owner decision.
- R13 backlog is post-MVP growth work and not required for current paper-safe runtime.
- [DEFERRED] No asyncio.timeout on polymarket.get_markets() in market_signal_scanner.py; scanner stall risk on hung HTTP call; P2, no capital impact.
- [DEFERRED] Migration 024 blast radius understated as test-user-only in forge report; SQL promotes all users; documentation drift, code is correct.
- [DEFERRED] PortfolioPage analytics UI note "market_expired excluded" may not match /portfolio/analytics backend query (pre-existing behavior, not introduced by this PR). Separate analytics backend cleanup lane required.
- [RESOLVED] RLS enabled on all 42 public tables — anon/authenticated locked out (migration 046).

# WARP•SENTINEL — CrusaderBot Re-Audit 2026-05-22

**Mode:** CORE AUDIT re-run (explicit WARP🔹CMD request)
**Issue:** #1276
**Branch:** `WARP/sentinel-reaudit-2026-05-22`
**Audit base:** `main` @ `a246c8f` (post WARP-62+63+64+65; fix commit `5f84646` confirmed in history)
**Prior verdict:** BLOCKED (60/100) — `core-audit-2026-05-21.md`
**Environment:** prod posture (ENFORCED)
**Tier:** MAJOR
**Auditor:** WARP•SENTINEL

---

## 1. Re-Audit Summary

The two blocking findings from issue #1271 are remediated and verified against code (not report claims):

- **F-001 (P0, H2 threading breach)** — FIXED. `domain/strategy/registry.py` no longer imports `threading`; the singleton is an eager module-level instance.
- **F-002 (P1, copy_targets drift in the signal scanner)** — CLEARED. `domain/signal/copy_trade.py` now reads the canonical `copy_trade_tasks` table.

Full test suite is green (**1613 passed, 1 skipped, 0 failures**), including the 9 new hermetic WARP-63 tests, the 36-test confluence scalper suite that depends on `_reset_for_tests()`, and the WebTrader exposure suite. All eight safety invariants H1–H8 now PASS — zero hard-rule breaches.

One residual architectural item remains (non-blocking): two `CopyTradeStrategy` implementations coexist, and the legacy `copy_targets` table is still *written* by the legacy `/copytrade` wizard while no scanner *reads* it anymore. This is a functional dead-end, not a safety risk, and was already tracked as the deferred F-002 cleanup lane.

Live Fly.io `/health` could not be probed — the execution environment's network policy blocks `crusaderbot.fly.dev` ("Host not in allowlist"). The health endpoint *code* is intact and unchanged since WARP-56; runtime liveness is reported UNVERIFIED rather than scored as failure.

**Critical findings:** 0.
**Verdict:** **APPROVED** — Score **96/100**.

---

## 2. WARP-62 Fix Verification (H2 Re-Evaluation)

**Target:** `projects/polymarket/crusaderbot/domain/strategy/registry.py`

| Check | Result | Evidence |
|-------|--------|----------|
| `import threading` absent | PASS | No `threading` import anywhere in file (imports are `re`, `typing`, `.base`, `.types` at lines 8–14). Repo-wide grep: only docstring mentions of "no threading" in 4 files; zero real imports outside `tests/`/`archive/`. |
| `_DEFAULT_REGISTRY = StrategyRegistry()` at module bottom | PASS | `registry.py:182`. |
| `instance()` returns `_DEFAULT_REGISTRY` | PASS | `registry.py:29-32` — classmethod returns the module global, no lock, no double-checked init. |
| `_reset_for_tests()` uses `global _DEFAULT_REGISTRY` | PASS | `registry.py:34-41` — rebinds the global to a fresh `cls()`. |
| No `threading.Lock` / `Lock()` usages | PASS | grep clean; the prior lines 30/39/52 lock sites are gone. |

**H2 HARD RULE re-evaluation: PASS.** The CLAUDE.md invariant *"Concurrency: asyncio only — never threading"* is satisfied. Eager module-level instantiation is correct for a single-process, single-event-loop runtime; `bootstrap_default_strategies()` remains idempotent and continues to register all four strategies via the public `get()`/`register()` boundary (`registry.py:141-172`).

---

## 3. WARP-63 Fix Verification (F-002 Re-Evaluation)

**Target:** `projects/polymarket/crusaderbot/domain/signal/copy_trade.py`

| Check | Result | Evidence |
|-------|--------|----------|
| `copy_targets` absent | PASS | grep over file: zero matches. SQL at `copy_trade.py:21-24` reads `copy_trade_tasks`. |
| `copy_trade_tasks` used | PASS | `copy_trade.py:22-23` — `SELECT id, wallet_address, copy_amount FROM copy_trade_tasks WHERE user_id=$1 AND status='active'`. |
| `wallet_address` canonical column | PASS | `copy_trade.py:22,36,39,63` — uses `wallet_address`; no `target_wallet_address`. |
| `status='active'` filter | PASS | `copy_trade.py:23`. |
| `copy_amount` canonical column | PASS | `copy_trade.py:22,52`; no `scale_factor`. |
| `last_seen_tx` write-back removed | PASS | grep clean — the legacy `UPDATE copy_targets SET last_seen_tx` path is gone. |
| Per-wallet exception isolation retained | PASS | `copy_trade.py:37-40` — fetch error logged at WARNING, loop continues. |

**F-002 drift re-evaluation: CLEARED.** The signal-scan `CopyTradeStrategy` (`domain/signal/copy_trade.py`, wired at `scheduler.py:26,237,257`) now reads the same canonical table as the production monitor (`services/copy_trade/monitor.py` → `domain/copy_trade/repository.list_active_tasks`). The double-table hazard described in the prior audit (legacy scanner reading a different table) no longer exists — both paths read `copy_trade_tasks`.

**Residual architectural note (non-blocking, was the deferred F-002 cleanup lane):**
- Two `CopyTradeStrategy` classes still coexist: the lightweight `domain/signal/copy_trade.py` (used by `scheduler.py`'s `_strategies` dict) and the richer `domain/strategy/strategies/copy_trade.py` (bootstrapped into the registry; adds `copy_trade_idempotency` dedup, `copy_mode`/`copy_direction`/market-filter handling). Both now read `copy_trade_tasks`, so there is no cross-table inconsistency, but the duplication is latent maintenance risk.
- The legacy `copy_targets` table is still *written* by `bot/handlers/copy_trade.py:519/524/534` (legacy `/copytrade` wizard) and `bot/handlers/setup.py:431`, but is no longer *read* by any scanner. Rows written there are now orphaned — the legacy wizard silently no longer mirrors. Functional dead-end, not a safety defect.
- Recommendation (keep as a follow-up lane, NOT a blocker): delete `domain/signal/copy_trade.py`'s class in favor of the registry strategy, then run the deferred `DROP TABLE copy_targets` migration and remove the two legacy write sites.

---

## 4. Test Suite Verification

Environment: `pytest 9.0.2`, deps installed from `pyproject.toml` into the runner venv. Run from repo root.

**Targeted re-audit run (issue Priority 3):**

| File | Result | Notes |
|------|--------|-------|
| `tests/test_warp63_copytrade_strategy_scan.py` | PASS | 9 hermetic tests. 5 source-level assertions (`copy_targets` absent, `copy_trade_tasks`/`wallet_address`/`copy_amount` present, `target_wallet_address`/`scale_factor`/`last_seen_tx` absent) + 4 behavioural (empty-tasks, zero-size skip, wallet-fetch-error continue, SQL targets `copy_trade_tasks` with `user_id` param). Mocks valid (AsyncMock pool/conn), imports resolve. |
| `tests/test_confluence_scalper.py` | PASS | Uses `StrategyRegistry._reset_for_tests()` (lines 63,65) and `StrategyRegistry.instance()` (line 228) — confirmed compatible with the new eager init; reset rebinds the global cleanly, no stale singleton leakage between tests. |
| `tests/test_webtrader_confluence_scalper_exposure.py` | PASS | Preset-isolation / exposure regression suite green. |

Combined targeted run: **85 passed in 1.33s**.

**Full suite (regression sweep for WARP-62+63):**

```
1613 passed, 1 skipped, 23 warnings in 91.33s
```

Zero failures. The 23 warnings are pre-existing `PytestWarning` (asyncio mark on sync tests in `test_order_lifecycle.py`) and `PTBUserWarning` (`per_message=False` ConversationHandler in `copy_trade.py`) — neither introduced by this fix cycle, neither a correctness issue. No dead references, no broken mocks, no import errors after the registry eager-init change.

---

## 5. Bot Runtime Health (Fly.io `/health`)

**Status: UNVERIFIED — network-policy blocked, not a failure.**

`GET https://crusaderbot.fly.dev/health` from this execution environment returns `Host not in allowlist` — the environment's outbound network policy does not permit `fly.dev`. Live `status`/`mode`/`version`/registry/DB-pool fields could not be observed.

Static evidence (code path intact):
- `api/health.py:121-144` `/health` returns `status`, `uptime_seconds`, `version` (`_resolve_version()`, line 138), `mode` (`_resolve_mode()`, line 139), `service`, `checks`, `ready`. Deep-deps result comes from `run_health_checks()` (line 123) which populates `checks` (DB pool, etc.).
- `api/health.py` unchanged since WARP-56 (`7755d94`) — outside the WARP-62/63 blast radius, so no regression risk from this fix cycle.
- fly.toml guards confirmed paper-only: `ENABLE_LIVE_TRADING="false"`, `EXECUTION_PATH_VALIDATED="false"`, `CAPITAL_MODE_CONFIRMED="false"` (`fly.toml:24-26`).

WARP🔹CMD should confirm live `/health` out-of-band (or via an allowlisted environment) before treating runtime liveness as proven. Code-level readiness: PASS.

---

## 6. Safety Invariants H1–H8 (Full Re-Run)

| # | Rule | Status | Evidence |
|---|------|--------|----------|
| H1 | No `except: pass` in runtime path | **PASS** | grep over `**/*.py` (excl. `tests/`): zero real matches. Only hit is the documenting docstring at `domain/execution/exit_watcher.py:31`. |
| H2 | No `threading` in async context | **PASS** (was P0 FAIL) | `domain/strategy/registry.py` threading import + locks removed; eager `_DEFAULT_REGISTRY` at line 182. Repo-wide: zero real `threading` usages outside tests/archive (4 remaining hits are docstrings stating the rule). **Fixed by WARP-62.** |
| H3 | No hardcoded secrets | **PASS** | No `key/secret/password = "<literal>"` outside `os.getenv`/`os.environ`. Unchanged by this cycle. |
| H4 | No full Kelly (`a=1.0`) | **PASS** | `domain/risk/constants.py` caps kelly at 0.25 (aggressive); `gate.py` + `hardening.py` enforce `0 < kelly <= K.KELLY_FRACTION`. Unchanged. |
| H5 | `ENABLE_LIVE_TRADING=false` | **PASS** | `fly.toml:24`. |
| H6 | `EXECUTION_PATH_VALIDATED=false` | **PASS** | `fly.toml:25`. |
| H7 | `CAPITAL_MODE_CONFIRMED=false` | **PASS** | `fly.toml:26`. |
| H8 | No `phase*/` folders | **PASS** | `find ... -type d -name 'phase*'` returns zero. |

**No regression introduced by WARP-62+63 across H1–H8. All eight PASS. Zero hard-rule breaches.**

---

## 7. Deferred Items Assessment (F-003, F-004)

| ID | Severity | Location | State | Recommendation |
|----|----------|----------|-------|----------------|
| F-003 | P2 | `main.py:76-77` | UNCHANGED | `bootstrap_default_strategies()` then `seed_defaults()` — the latter is an alias for the former (`registry.py:175-179`). Idempotent and harmless; one redundant call at boot only. **Keep deferred.** Trivial one-line cleanup whenever `main.py` is next touched. |
| F-004 | P3 | `bot/middleware/access_tier.py` | UNCHANGED | Filename retained for import-path stability after the `access_tier → role` rename; module body is role-based (docstring line 1 confirms). Cosmetic stale signal only. **Keep deferred.** Rename in a dedicated lane with import updates. |

Neither deferred item gained severity. Neither blocks APPROVED. The new residual architectural note in §3 (dual `CopyTradeStrategy` + orphaned `copy_targets` writes) is the natural successor to the original F-002 cleanup lane and is recommended as the next follow-up — at **P2** (no safety impact; legacy mirror path is inert, not dangerous).

---

## 8. Stability Score (0–100)

| Domain | Weight | Score | Notes |
|--------|-------:|------:|-------|
| Architecture | 20% | 18/20 | Clean domain layout; H2 drift resolved. -2 for residual dual `CopyTradeStrategy` class + orphaned `copy_targets` write sites (inert, but latent maintenance risk). |
| Functional (scheduler, gate, router) | 20% | 20/20 | All paths intact; full suite green (1613 passed). |
| Failure modes (retry, dedup, FK demote) | 20% | 20/20 | RetryAfter cap, idempotency window, FK→DEBUG, per-wallet/per-user isolation all intact; orphan-table hazard eliminated. |
| Risk rules in code | 20% | 20/20 | Kelly capped 0.25, full Kelly impossible, 5-guard live gate, H2 breach removed — zero hard-rule FAIL. |
| Infra + Telegram | 10% | 9/10 | Alert paths + fly.toml guards intact; -1 because live `/health` could not be probed (network allowlist), code-level only. |
| Latency (declared budgets, not measured) | 10% | 9/10 | Job intervals reasonable; not measured this run. |
| **Subtotal** | **100%** | **96/100** | |
| Hard-rule breach penalty | — | **0** | H1–H8 all PASS — no penalty (prior cycle's -35 lifted). |
| **Final** | | **96/100** | |

---

## 9. Verdict

**Verdict:** `APPROVED`

**Rationale:** Both blocking findings from #1271 are remediated and code-verified. H2 (the sole prior critical) now PASSes; all eight H1–H8 invariants PASS with zero breaches. The full test suite is green (1613 passed, 0 failures), including the new WARP-63 hermetic tests and the `_reset_for_tests()`-dependent confluence scalper suite that exercises the new eager singleton init. Score 96/100 (≥ 85) with zero critical issues satisfies the APPROVED threshold.

**Conditions / follow-ups (none blocking):**
1. Live `/health` liveness is UNVERIFIED here (network allowlist) — confirm out-of-band before relying on runtime status.
2. P2 follow-up lane: collapse the dual `CopyTradeStrategy` implementations and run the deferred `DROP TABLE copy_targets` migration + remove the two legacy write sites (`bot/handlers/copy_trade.py`, `bot/handlers/setup.py`). Inert today, but it leaves the legacy `/copytrade` wizard writing rows nothing reads.
3. F-003 / F-004 remain low-priority deferred cleanups.

**The prior BLOCKED verdict is LIFTED.**

---

**Score: 96/100 | Verdict: APPROVED**

**NEXT GATE:** Return to WARP🔹CMD for merge decision (APPROVED — eligible to merge).
